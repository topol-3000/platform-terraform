---
phase: quick-260624-gy7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-260624-gy7
must_haves:
  truths:
    - "README status callout reflects v1.1 complete (not scaffold/stubs)"
    - "All region references say us-east-1"
    - "ECR module is described as a managed repository (not pull-through cache)"
    - "Makefile section lists all 8 targets including plan-check"
    - "Outputs table contains vpc_id, alb_security_group_id, control_plane_rds_endpoint and has no placeholder footnote"
    - "Roadmap section reflects all modules complete"
    - "New tunable variables are listed"
    - "docs/github-actions-ecr-push.md is referenced"
  artifacts:
    - path: "README.md"
      provides: "Up-to-date project README"
  key_links:
    - from: "README.md"
      to: "envs/prod/variables.tf"
      via: "region default, variable names"
    - from: "README.md"
      to: "Makefile"
      via: "make targets"
    - from: "README.md"
      to: "envs/prod/outputs.tf"
      via: "outputs table"
---

<objective>
Rewrite README.md to reflect the completed v1.1 milestone state of the repo.

Purpose: The README was written at scaffold stage and contains 8 specific factual errors
(wrong status, wrong region, wrong ECR description, incomplete Makefile targets, stale outputs
table, pending roadmap, missing variables, missing docs reference). Fix all 8 without changing
the README's structure or tone.

Output: README.md with all facts matching the actual codebase.
</objective>

<execution_context>
@/home/yevhenii/.claude/get-shit-done/workflows/execute-plan.md
@/home/yevhenii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Key facts extracted from the codebase (verified before this plan was written):

REGION: `us-east-1` — confirmed in envs/prod/variables.tf (default = "us-east-1"),
envs/prod/backend.tf (region = "us-east-1"), envs/prod/terraform.tfvars.example (region = "us-east-1").
The README currently says eu-central-1 in two places; both must be changed.
Note: docs/github-actions-ecr-push.md still contains eu-central-1 in its code examples —
do NOT touch that doc in this task.

ECR: modules/ecr/main.tf creates aws_ecr_repository.odoo_core (managed repo, IMMUTABLE tags,
scan_on_push, AES256, lifecycle expiry 14 days). No pull-through cache. D-01 changed the
architecture. The CI/CD push guide is at docs/github-actions-ecr-push.md.

MAKEFILE targets (from Makefile, exact names):
  help, bootstrap, init, plan, plan-check, apply, destroy, fmt, validate, clean
  Notable: plan-check is the offline fmt/validate/plan gate — no AWS/S3 access needed;
  it writes a transient gate_override.tf with stubbed provider + local backend, runs
  fmt -check + validate + plan, then always cleans up.

OUTPUTS (from envs/prod/outputs.tf — all are live, none commented out):
  ecs_cluster_arn, private_subnet_ids, task_security_group_id, vpc_id,
  alb_security_group_id, alb_listener_arn, tenant_rds_endpoint, rds_proxy_endpoint,
  control_plane_rds_endpoint, efs_id, hosted_zone_id, acm_cert_arn, ecr_image_uri

VARIABLES in envs/prod/variables.tf:
  region (default us-east-1), environment, project, tenant_domain,
  vpc_cidr (default 10.0.0.0/16), azs (default [us-east-1a, us-east-1b]),
  enable_rds_proxy (default false), rds_instance_class (default db.t4g.small),
  rds_engine_version (default "16"), rds_allocated_storage (default 20),
  rds_max_allocated_storage (default 100)

MODULES (from envs/prod/main.tf — all wired, none commented out):
  networking, ecr, ecs, ssm, rds_tenant, rds_proxy, rds_control_plane,
  efs, acm, alb, route53
  rds_proxy is gated by var.enable_rds_proxy (default false; activate at ~30 tenants).

STATE: Milestone v1.1 complete. All 5 phases, 15 plans executed. terraform plan produces
real resources.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with all 8 factual corrections</name>
  <files>README.md</files>
  <action>
Rewrite README.md addressing every item below. Preserve the existing section headings,
the SEED-001 link, the chicken-and-egg explanation, the backend section, the prerequisites
section structure, and the quick start flow. Only change what is factually wrong.

1. STATUS CALLOUT: Remove the blockquote starting "> **Status:** scaffold." Replace with:

   > **Status:** v1.1 complete. All 11 modules are implemented and wired in `envs/prod/main.tf`.
   > `terraform plan` produces real resources. No `terraform apply` has been run — this repo
   > is code-complete; apply it to provision the actual AWS baseline.

2. LAYOUT BLOCK: In the module tree, change the ecr line from:
     `├── ecr/            # ECR pull-through cache for odoo-core (from GHCR)`
   to:
     `├── ecr/            # managed ECR repository for odoo-core (CI/CD pushes via GitHub Actions)`
   Add a docs/ entry to the layout tree at the same level as bootstrap/, envs/, modules/:
     `└── docs/           # supplementary guides (e.g. github-actions-ecr-push.md)`

3. REGION: In the Prerequisites section, change `eu-central-1` to `us-east-1`.
   Scan the rest of the README; fix any remaining occurrence of `eu-central-1`.

4. QUICK START / MAKEFILE: After the existing paragraph "A Makefile wraps the common
   commands — `make plan`, `make apply`, `make fmt`, `make validate` (all operate on
   `envs/prod`), and `make bootstrap`." replace it with:

   A `Makefile` wraps the common commands (all operate on `envs/prod` unless noted):

   | Target | Description |
   |--------|-------------|
   | `make help` | List all targets with descriptions |
   | `make bootstrap` | Create the S3 state bucket (run once, local state) |
   | `make init` | `terraform init` for the prod env |
   | `make plan-check` | **Offline gate:** `fmt -check` + `validate` + non-empty `plan` — no AWS credentials or S3 access required |
   | `make plan` | `terraform plan` (real: requires S3 backend + AWS credentials) |
   | `make apply` | `terraform apply` |
   | `make destroy` | `terraform destroy` |
   | `make fmt` | Format all `.tf` files recursively |
   | `make validate` | `terraform validate` |
   | `make clean` | Remove local `.terraform` dirs |

   `make plan-check` is the code-complete verification gate — use it to confirm the config
   is well-formed without needing AWS access. It writes a transient `gate_override.tf`
   (local backend + stubbed AWS provider), runs the checks, then cleans up.

5. OUTPUTS TABLE: Replace the entire outputs section content (table + "These are
   placeholders..." sentence) with the following table. Preserve the section heading
   "## Outputs -> provisioner settings" and the intro paragraph above the table:

   | Terraform output             | provisioner setting                    |
   |------------------------------|----------------------------------------|
   | `ecs_cluster_arn`            | `aws_ecs_cluster`                      |
   | `private_subnet_ids`         | `aws_subnets`                          |
   | `task_security_group_id`     | `aws_security_groups`                  |
   | `vpc_id`                     | (consumed by downstream modules)       |
   | `alb_security_group_id`      | (consumed by downstream modules)       |
   | `alb_listener_arn`           | `aws_alb_listener_arn`                 |
   | `tenant_rds_endpoint`        | `aws_shared_rds_endpoint`              |
   | `rds_proxy_endpoint`         | `aws_rds_proxy_endpoint`               |
   | `control_plane_rds_endpoint` | `aws_control_plane_rds_endpoint`       |
   | `efs_id`                     | `aws_efs_id`                           |
   | `hosted_zone_id`             | `aws_hosted_zone_id`                   |
   | `acm_cert_arn`               | `aws_acm_cert_arn`                     |
   | `ecr_image_uri`              | `aws_ecr_image`                        |

6. ROADMAP SECTION: Replace the entire Roadmap section with a module-status summary:

   ## Module status

   All modules are implemented and wired in `envs/prod/main.tf` (v1.1 complete):

   | Module | Description | Status |
   |--------|-------------|--------|
   | `networking` | VPC, public subnets (no NAT gateway), security groups | complete |
   | `ecr` | Managed ECR repo for odoo-core (IMMUTABLE tags, scan on push) | complete |
   | `ecs` | Shared Fargate cluster | complete |
   | `ssm` | SSM Parameter Store SecureStrings (HMAC salt, RDS master creds) | complete |
   | `rds-tenant` | Shared Single-AZ PostgreSQL; one database per tenant | complete |
   | `rds-proxy` | RDS Proxy fronting rds-tenant; gated by `enable_rds_proxy` (activate at ~30 tenants) | complete |
   | `rds-control-plane` | Separate Multi-AZ PostgreSQL for control-plane (provisioner) data | complete |
   | `efs` | Shared EFS; per-tenant access points created by the provisioner adapter | complete |
   | `alb` | Shared ALB, host-based routing, idle timeout > 60 s (Odoo longpoll) | complete |
   | `acm` | Wildcard ACM cert for `*.{tenant_domain}` | complete |
   | `route53` | Hosted zone for the tenant domain | complete |

7. VARIABLES: Add a new "## Configuration" section (place it after Quick start, before
   Outputs) listing the key tunable variables:

   ## Configuration

   Copy `envs/prod/terraform.tfvars.example` to `envs/prod/terraform.tfvars` (gitignored)
   and set the values for your deployment:

   | Variable | Default | Notes |
   |----------|---------|-------|
   | `region` | `us-east-1` | Must match the state bucket region |
   | `environment` | `prod` | Used in resource names and tags |
   | `project` | `odoo-saas` | Resource name prefix |
   | `tenant_domain` | _(required)_ | Apex domain, e.g. `saas.example.com` — set before applying route53/acm/alb |
   | `vpc_cidr` | `10.0.0.0/16` | CIDR block for the VPC |
   | `azs` | `[us-east-1a, us-east-1b]` | Availability zones for public subnets |
   | `enable_rds_proxy` | `false` | Set `true` at ~30 active tenants |
   | `rds_instance_class` | `db.t4g.small` | Applies to both tenant and control-plane RDS |
   | `rds_engine_version` | `16` | PostgreSQL major version |
   | `rds_allocated_storage` | `20` | Initial storage in GiB per RDS instance |
   | `rds_max_allocated_storage` | `100` | Storage autoscaling ceiling in GiB |

8. DOCS REFERENCE: In the Layout section, after the layout tree block, add a line:

   See [`docs/github-actions-ecr-push.md`](docs/github-actions-ecr-push.md) for the guide
   on setting up GitHub Actions to push the `odoo-core` image to the ECR repository.

   (Place this before the "Why directory-per-env" paragraph.)
  </action>
  <verify>
After writing the file, grep to confirm the 8 corrections:

1. `grep -c "scaffold" README.md` returns 0 (no remaining "scaffold" references in the status callout)
2. `grep "us-east-1" README.md` shows at least 2 hits; `grep "eu-central-1" README.md` returns 0
3. `grep "pull-through" README.md` returns 0
4. `grep "plan-check" README.md` shows at least 1 hit
5. `grep "control_plane_rds_endpoint" README.md` shows at least 1 hit
6. `grep "vpc_id" README.md` shows at least 1 hit (in outputs table)
7. `grep "enable_rds_proxy" README.md` shows at least 1 hit (in variables table)
8. `grep "github-actions-ecr-push" README.md` shows at least 1 hit
  </verify>
  <done>
README.md has no scaffold/stub language, no eu-central-1 references, describes ECR as a managed
repository, lists all 10 Makefile targets, has a 13-row outputs table with no placeholder footnote,
has a module-status table showing all 11 modules complete, has a configuration table with all 11
variables, and references docs/github-actions-ecr-push.md.
  </done>
</task>

</tasks>

<verification>
Sanity-check each factual claim against its source file:

- Region: envs/prod/variables.tf line 5 (`default = "us-east-1"`) + backend.tf line 10 (`region = "us-east-1"`)
- Make targets: Makefile `.PHONY` line: `help bootstrap init plan plan-check apply destroy fmt validate clean` (10 targets)
- Outputs: envs/prod/outputs.tf has 13 output blocks, all uncommented
- Variables: envs/prod/variables.tf has 11 variable blocks
- Modules: envs/prod/main.tf has 11 module blocks, all uncommented
- ECR type: modules/ecr/main.tf creates `aws_ecr_repository` (not pull-through cache)
- Docs file: docs/github-actions-ecr-push.md exists
</verification>

<success_criteria>
- README.md contains zero occurrences of "eu-central-1"
- README.md contains zero occurrences of "scaffold" (in the status/stub sense)
- README.md contains zero occurrences of "pull-through cache"
- README.md contains zero occurrences of "These are placeholders"
- README.md references make plan-check with a description of what it does
- README.md has a 13-row outputs table (vpc_id, alb_security_group_id, control_plane_rds_endpoint added)
- README.md has a module-status table with 11 rows, all marked complete
- README.md has a configuration table with 11 variables
- README.md references docs/github-actions-ecr-push.md
</success_criteria>

<output>
Create `.planning/quick/260624-gy7-update-readme/260624-gy7-SUMMARY.md` when done.
</output>
