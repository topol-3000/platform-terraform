---
phase: 05-tls-and-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - envs/prod/main.tf
  - envs/prod/outputs.tf
autonomous: true
requirements:
  - ACM-01
  - ALB-01
  - DNS-01
  - TLS-02

must_haves:
  truths:
    - "module acm is uncommented and wired with name_prefix and tenant_domain in envs/prod/main.tf"
    - "module alb is uncommented and expanded with acm_cert_arn, subnet_ids, security_group_id — appears AFTER module acm in the file"
    - "module route53 is uncommented and wired with name_prefix and tenant_domain in envs/prod/main.tf"
    - "acm_cert_arn, alb_listener_arn, and hosted_zone_id outputs are uncommented in envs/prod/outputs.tf"
    - "make plan-check exits 0 and plan output includes ACM, ALB, and Route53 resources"
    - "full provisioner output contract is satisfied — all nine outputs resolve"
  artifacts:
    - path: "envs/prod/main.tf"
      provides: "active module calls for acm, alb, route53 with complete argument wiring"
      contains: "module.acm.cert_arn"
    - path: "envs/prod/outputs.tf"
      provides: "three uncommented outputs: acm_cert_arn, alb_listener_arn, hosted_zone_id"
      contains: "module.alb.listener_arn"
  key_links:
    - from: "envs/prod/main.tf module.alb"
      to: "envs/prod/main.tf module.acm"
      via: "acm_cert_arn = module.acm.cert_arn"
      pattern: "module\\.acm\\.cert_arn"
    - from: "envs/prod/outputs.tf"
      to: "envs/prod/main.tf"
      via: "module.alb.listener_arn / module.acm.cert_arn / module.route53.hosted_zone_id"
      pattern: "module\\.alb\\.listener_arn"
    - from: "envs/prod/main.tf module.alb"
      to: "envs/prod/main.tf module.networking"
      via: "module.networking.private_subnet_ids + module.networking.alb_security_group_id"
      pattern: "module\\.networking\\.alb_security_group_id"
---

<objective>
Wire modules/acm, modules/alb, and modules/route53 into envs/prod — uncomment and expand the three stub module calls in envs/prod/main.tf, uncomment the three contract outputs in envs/prod/outputs.tf — then run make plan-check to verify the full baseline (all 12 modules) produces a non-empty plan.

Purpose: This is the final plan of the final phase. Completing it means the full provisioner output contract is satisfied: cert_arn, listener_arn, and hosted_zone_id join the nine already-active outputs. The make plan-check gate confirms all 12 modules are wired correctly with a non-empty plan.

Output: Updated envs/prod/main.tf + envs/prod/outputs.tf. make plan-check exits 0 with ACM cert, ALB, and Route53 zone appearing in the plan alongside all prior-phase resources.
</objective>

<execution_context>
@/home/yevhenii/.claude/get-shit-done/workflows/execute-plan.md
@/home/yevhenii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tls-and-routing/05-CONTEXT.md
@.planning/phases/05-tls-and-routing/05-RESEARCH.md
@.planning/phases/05-tls-and-routing/05-PATTERNS.md
@.planning/phases/05-tls-and-routing/05-01-SUMMARY.md
@.planning/phases/05-tls-and-routing/05-02-SUMMARY.md

<interfaces>
<!-- Key module outputs already active in envs/prod that will be threaded into new modules. -->
<!-- Verified from modules/networking/outputs.tf and envs/prod/outputs.tf. -->

From envs/prod/main.tf (active outputs from Phase 1 networking module):
  module.networking.private_subnet_ids   → list(string) of public subnet IDs (contract name retained for provisioner)
  module.networking.alb_security_group_id → string; ALB SG allowing ingress 80/443 from internet (Phase 1, active)
  module.networking.vpc_id               → string; VPC id (not needed for acm/alb/route53 wiring)

From modules/acm/outputs.tf (created in plan 05-01):
  module.acm.cert_arn → string; wildcard ACM certificate ARN; feeds into alb acm_cert_arn argument

From modules/alb/outputs.tf (created in plan 05-02):
  module.alb.listener_arn → string; HTTPS:443 listener ARN; re-exported in envs/prod/outputs.tf

From modules/route53/outputs.tf (created in plan 05-01):
  module.route53.hosted_zone_id → string; Route53 zone ID; re-exported in envs/prod/outputs.tf

Current commented stubs in envs/prod/main.tf (lines 92-108):
  module "acm" stub: has name_prefix + tenant_domain (correct)
  module "alb" stub: has name_prefix + acm_cert_arn ONLY — MISSING subnet_ids and security_group_id
  module "route53" stub: has tenant_domain but MISSING name_prefix

Current commented outputs in envs/prod/outputs.tf:
  lines 31-34: alb_listener_arn → module.alb.listener_arn
  lines 56-59: hosted_zone_id → module.route53.hosted_zone_id
  lines 61-64: acm_cert_arn → module.acm.cert_arn

WIRING ORDER: module "acm" MUST appear before module "alb" in envs/prod/main.tf
              because module "alb" references module.acm.cert_arn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uncomment and expand module calls in envs/prod/main.tf</name>
  <files>envs/prod/main.tf</files>
  <read_first>
    envs/prod/main.tf (full file — understand current structure, locate the commented stubs at lines 92-108)
    .planning/phases/05-tls-and-routing/05-PATTERNS.md (section: envs/prod/main.tf — exact wiring with expanded alb stub and route53 with name_prefix)
    modules/networking/outputs.tf (confirm module.networking.alb_security_group_id and private_subnet_ids are active outputs)
    envs/prod/outputs.tf (confirm existing active outputs; understand file structure before editing)
  </read_first>
  <action>
    In envs/prod/main.tf, replace the commented stub block (lines 92-108, the three #-commented module blocks) with active module calls. The wiring must be:

    module "acm" — source = "../../modules/acm", name_prefix = local.name_prefix, tenant_domain = var.tenant_domain. This is the stub as-is; just uncomment it (remove # prefix from each line).

    module "alb" — source = "../../modules/alb", name_prefix = local.name_prefix, acm_cert_arn = module.acm.cert_arn, subnet_ids = module.networking.private_subnet_ids, security_group_id = module.networking.alb_security_group_id. The stub is INCOMPLETE (it only has name_prefix and acm_cert_arn); expand it with the two missing arguments. module "alb" MUST appear after module "acm" in the file (wiring order: acm before alb so module.acm.cert_arn resolves).

    module "route53" — source = "../../modules/route53", name_prefix = local.name_prefix, tenant_domain = var.tenant_domain. The stub omits name_prefix; add it (matches the declared variable in modules/route53/variables.tf from plan 05-01). module "route53" may appear after module "alb" or alongside it (no dependency between alb and route53).

    Keep the "# Build step 6 — Routing/TLS" comment header above the acm block to document build order. Remove the outer comment markers (lines of just "#") that wrap the stubs. The result should be three clean, active module blocks with no # prefixes.
  </action>
  <verify>
    <automated>grep -c 'module.acm.cert_arn' /home/yevhenii/projects/saas/platform-terraform/envs/prod/main.tf</automated>
  </verify>
  <acceptance_criteria>
    - envs/prod/main.tf contains active (uncommented) module "acm" block with source, name_prefix, tenant_domain
    - envs/prod/main.tf contains active module "alb" block with source, name_prefix, acm_cert_arn = module.acm.cert_arn, subnet_ids = module.networking.private_subnet_ids, security_group_id = module.networking.alb_security_group_id
    - envs/prod/main.tf contains active module "route53" block with source, name_prefix, tenant_domain
    - module "acm" block appears BEFORE module "alb" block in the file (line number of acm < line number of alb)
    - No commented-out # module "acm" / # module "alb" / # module "route53" stubs remain in the file
    - grep -c 'module.acm.cert_arn' envs/prod/main.tf returns 1
    - grep -c 'module.networking.alb_security_group_id' envs/prod/main.tf returns 1 (in the alb module call)
  </acceptance_criteria>
  <done>Three module calls active in envs/prod/main.tf: acm (tenant_domain + name_prefix), alb (name_prefix + acm_cert_arn + subnet_ids + security_group_id), route53 (tenant_domain + name_prefix). Wiring order: acm before alb.</done>
</task>

<task type="auto">
  <name>Task 2: Uncomment contract outputs and run make plan-check</name>
  <files>envs/prod/outputs.tf</files>
  <read_first>
    envs/prod/outputs.tf (full file — locate the three commented output blocks at lines 31-34, 56-59, 61-64)
    .planning/phases/05-tls-and-routing/05-PATTERNS.md (section: envs/prod/outputs.tf — exact output blocks to uncomment)
  </read_first>
  <action>
    In envs/prod/outputs.tf, uncomment the three commented output blocks by removing the # prefix from each line:

    alb_listener_arn (currently lines 31-34): uncomment the block. Value = module.alb.listener_arn. Description already correct: "ALB HTTPS listener ARN -> provisioner `aws_alb_listener_arn`."

    hosted_zone_id (currently lines 56-59): uncomment the block. Value = module.route53.hosted_zone_id. Description already correct: "Route53 zone id -> provisioner `aws_hosted_zone_id`."

    acm_cert_arn (currently lines 61-64): uncomment the block. Value = module.acm.cert_arn. Description already correct: "Wildcard ACM cert ARN -> provisioner `aws_acm_cert_arn`."

    After uncommenting, run make plan-check from the repo root. The plan-check target: (1) runs terraform fmt -check -recursive, (2) runs terraform validate in envs/prod, (3) runs terraform plan -input=false -var "tenant_domain=placeholder.example.com" (with the dummy domain injected per plan 05-02). All three steps must exit 0 and the plan must be non-empty (shows ACM certificate, ALB, Route53 zone resources alongside all prior-phase resources).
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && make plan-check</automated>
  </verify>
  <acceptance_criteria>
    - envs/prod/outputs.tf contains active (uncommented) output "alb_listener_arn" with value = module.alb.listener_arn
    - envs/prod/outputs.tf contains active output "hosted_zone_id" with value = module.route53.hosted_zone_id
    - envs/prod/outputs.tf contains active output "acm_cert_arn" with value = module.acm.cert_arn
    - No commented-out # output "alb_listener_arn" / # output "hosted_zone_id" / # output "acm_cert_arn" remain in the file
    - make plan-check exits 0 (terraform fmt -check passes, terraform validate passes, terraform plan is non-empty)
    - terraform plan output includes at least one of: aws_acm_certificate, aws_lb, aws_lb_listener, aws_route53_zone
    - terraform plan output contains more resources than the Phase 4 baseline (31 resources) — 5+ new resources from ACM, ALB (2 listeners), Route53 bring the total to 36+
    - All nine provisioner contract outputs resolve: ecs_cluster_arn, private_subnet_ids, task_security_group_id, alb_listener_arn, tenant_rds_endpoint, rds_proxy_endpoint, efs_id, hosted_zone_id, acm_cert_arn
  </acceptance_criteria>
  <done>Three contract outputs uncommented in envs/prod/outputs.tf. make plan-check exits 0. Full provisioner output contract satisfied: all nine outputs resolve. ACM cert, ALB (+ 2 listeners), and Route53 zone appear in the plan. Phase 5 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| envs/prod/main.tf module arguments | All cross-module references flow through root config; no module calls another module |
| envs/prod/outputs.tf → provisioner | Outputs are non-secret identifiers (ARNs, IDs); no credentials or key material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | module "alb" acm_cert_arn wired to module.acm.cert_arn | accept | Wiring is in-root config; no external input path. At plan time, value is (known after apply) for the offline gate — no validation possible or needed. At real apply time ACM ARN is a valid AWS ARN. |
| T-05-12 | Info Disclosure | envs/prod/outputs.tf exports cert_arn, listener_arn, hosted_zone_id | accept | These are non-secret resource identifiers, not credentials. The provisioner needs them to attach tenant resources. No SSM parameter values, no passwords, no private key material in any output (consistent with prior phase posture). |
| T-05-13 | Elevation | Wiring expands the attack surface (ALB now routes public traffic) | accept | This phase is plan-only/offline — no real ALB is created. At real apply time, the ALB SG (Phase 1) already limits ingress to 80/443. The task SG limits 8069 to ALB SG. No additional exposure introduced by wiring. |
| T-05-14 | Tampering | Route53 public zone exposes DNS | accept | Zone declared but not applied this phase. At real apply time, AWS issues NS records. No records exist in the zone (per D-03); the zone itself is not a secret. Low-value target without applied tenant infrastructure. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all resources use the already-pinned hashicorp/aws 6.51.0 provider |
</threat_model>

<verification>
Final gate — run from repo root after both tasks complete:

```
make plan-check
```

Expected: exits 0. Plan output includes aws_acm_certificate.wildcard, aws_lb.main, aws_lb_listener.http, aws_lb_listener.https, aws_route53_zone.main in addition to all prior-phase resources. All outputs section shows: ecs_cluster_arn, private_subnet_ids, task_security_group_id, alb_listener_arn, tenant_rds_endpoint, rds_proxy_endpoint, efs_id, hosted_zone_id, acm_cert_arn — all known-after-apply or actual values.

Also run independently to confirm format gate:
```
terraform fmt -check -recursive
```
</verification>

<success_criteria>
- envs/prod/main.tf: three active module calls (acm, alb, route53); alb call includes subnet_ids = module.networking.private_subnet_ids and security_group_id = module.networking.alb_security_group_id; acm appears before alb
- envs/prod/outputs.tf: nine active outputs including the three new ones (alb_listener_arn, hosted_zone_id, acm_cert_arn); no commented-out output blocks remain
- make plan-check exits 0: terraform fmt -check -recursive passes, terraform validate passes, terraform plan is non-empty with ACM + ALB + Route53 resources visible
- Resource count in plan is > 31 (Phase 4 baseline); expected ~36 (31 + acm_certificate + aws_lb + aws_lb_listener×2 + aws_route53_zone)
- Phase 5 TLS and routing is complete; milestone v1.1 "Complete the shared AWS baseline" is achieved
</success_criteria>

<output>
Create `.planning/phases/05-tls-and-routing/05-03-SUMMARY.md` when done.
</output>
