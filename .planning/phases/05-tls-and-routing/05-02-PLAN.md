---
phase: 05-tls-and-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/alb/main.tf
  - modules/alb/variables.tf
  - modules/alb/outputs.tf
  - Makefile
autonomous: true
requirements:
  - ALB-01
  - TLS-02

must_haves:
  truths:
    - "modules/alb declares a shared internet-facing ALB with idle_timeout > 60 and lean toggles: enable_deletion_protection=false, enable_http2=true, drop_invalid_header_fields=true, no access logs (D-04)"
    - "modules/alb declares HTTP:80 listener with 301 redirect to HTTPS (D-01)"
    - "modules/alb declares HTTPS:443 listener with ACM cert and fixed-response 503 default (D-01)"
    - "modules/alb exports listener_arn (the HTTPS:443 listener ARN)"
    - "Makefile plan-check injects -var tenant_domain=placeholder.example.com so the offline gate stays green when validation blocks land"
  artifacts:
    - path: "modules/alb/main.tf"
      provides: "aws_lb.main + aws_lb_listener.http (redirect) + aws_lb_listener.https (fixed-response 503)"
      contains: "aws_lb"
    - path: "modules/alb/variables.tf"
      provides: "name_prefix, acm_cert_arn, subnet_ids, security_group_id variables"
      contains: "acm_cert_arn"
    - path: "modules/alb/outputs.tf"
      provides: "listener_arn output pointing to aws_lb_listener.https.arn"
      exports: ["listener_arn"]
    - path: "Makefile"
      provides: "plan-check target injects -var \"tenant_domain=placeholder.example.com\""
      contains: "tenant_domain=placeholder.example.com"
  key_links:
    - from: "modules/alb/main.tf (aws_lb_listener.https)"
      to: "modules/alb/variables.tf"
      via: "var.acm_cert_arn"
      pattern: "certificate_arn\\s*=\\s*var\\.acm_cert_arn"
    - from: "modules/alb/outputs.tf"
      to: "modules/alb/main.tf"
      via: "aws_lb_listener.https.arn"
      pattern: "aws_lb_listener\\.https\\.arn"
    - from: "Makefile plan-check"
      to: "modules/acm + modules/route53 validation blocks"
      via: "-var tenant_domain=placeholder.example.com"
      pattern: "tenant_domain=placeholder\\.example\\.com"
---

<objective>
Implement modules/alb (the shared Application Load Balancer with two listeners) and update the Makefile plan-check target to inject the dummy tenant_domain value that will be required once modules/acm and modules/route53 validation blocks are active.

Purpose: The ALB is the HTTPS termination point for all tenant traffic. Its HTTPS:443 listener ARN is the primary provisioner contract output (aws_alb_listener_arn). The Makefile fix prevents the offline gate from breaking when the tenant_domain validation blocks land — this is the critical sequencing fix identified in RESEARCH.md Pitfall 1.

Output: Three ALB module files + a one-line Makefile change. The Makefile change is paired here (not in the wiring plan) because it must be in place before plan-check is run at the end of plan 05-03.
</objective>

<execution_context>
@/home/yevhenii/.claude/get-shit-done/workflows/execute-plan.md
@/home/yevhenii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tls-and-routing/05-CONTEXT.md
@.planning/phases/05-tls-and-routing/05-RESEARCH.md
@.planning/phases/05-tls-and-routing/05-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from existing codebase analogs. -->

From modules/networking/main.tf (multi-resource module structure):
  All resource names follow "${var.name_prefix}-<thing>" pattern.
  tags = { Name = "${var.name_prefix}-<thing>" } on the main resource.

From modules/rds-tenant/variables.tf (multi-variable file pattern):
  Required inputs (no defaults): subnet_ids list(string), vpc_id string — no validation blocks on non-domain inputs.
  variable "subnet_ids" {
    description = "Subnet IDs for the RDS DB subnet group. Requires >=2 subnets in different AZs."
    type        = list(string)
  }

From modules/rds-tenant/outputs.tf (single contractual output):
  output "endpoint" {
    description = "Shared tenant RDS endpoint -> provisioner `aws_shared_rds_endpoint`."
    value       = aws_db_instance.tenant.endpoint
  }

From Makefile line 48 (current plan-check terraform plan line):
  terraform plan -input=false
  -- Must become: terraform plan -input=false -var "tenant_domain=placeholder.example.com"

CRITICAL ATTRIBUTES:
  idle_timeout = 120 goes on aws_lb (the LB resource), NOT on aws_lb_listener
  ssl_policy is ONLY valid on HTTPS listeners, NOT on HTTP:80 listener
  aws_lb (NOT aws_alb — legacy alias)
  fixed_response is nested inside default_action block, NOT a top-level attribute
  ssl_policy value: "ELBSecurityPolicy-TLS13-1-2-2021-06"
  HTTPS listener status_code in fixed_response is string "503" not integer
  HTTP redirect status_code is string "HTTP_301" not "301"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement modules/alb (ALB + two listeners)</name>
  <files>modules/alb/main.tf, modules/alb/variables.tf, modules/alb/outputs.tf</files>
  <read_first>
    modules/alb/main.tf (current stub — replace STATUS/TODO lines, implement 3 resources)
    modules/alb/variables.tf (current stub — add 3 new required variables)
    modules/alb/outputs.tf (current stub — add listener_arn output)
    modules/networking/main.tf (analog: multi-resource module structure, name_prefix-<thing> naming, tags pattern)
    modules/rds-tenant/variables.tf (analog: multi-variable file with required inputs, no defaults on required inputs)
    modules/rds-tenant/outputs.tf (analog: single contractual output with provisioner mapping in description)
    .planning/phases/05-tls-and-routing/05-PATTERNS.md (sections: modules/alb/main.tf, modules/alb/variables.tf, modules/alb/outputs.tf — contains exact resource shapes)
    .planning/phases/05-tls-and-routing/05-RESEARCH.md (Pattern 2: aws_lb resource, Pattern 3: HTTPS listener, Pattern 4: HTTP redirect listener, Anti-Patterns section)
  </read_first>
  <action>
    Implement all three files for modules/alb:

    main.tf: Replace the stub. Update the header comment to STATUS: implemented. Keep SEED-001 note about idle_timeout > 60 and healthCheckGracePeriod >= 240s (the grace period is adapter-owned, document it here for reference). Declare three resources:

    1. aws_lb with label "main": name = "${var.name_prefix}-alb", internal = false, load_balancer_type = "application", security_groups = [var.security_group_id], subnets = var.subnet_ids, idle_timeout = 120 (must be > 60; Odoo longpoll guard; this attribute is on aws_lb NOT aws_lb_listener), enable_deletion_protection = false, enable_http2 = true, drop_invalid_header_fields = true (per D-04: free hardening on). No access_logs block (deferred per D-04). Add tags = { Name = "${var.name_prefix}-alb" } for the Name tag.

    2. aws_lb_listener with label "http": load_balancer_arn = aws_lb.main.arn, port = "80", protocol = "HTTP". Default action type = "redirect" with nested redirect block: port = "443", protocol = "HTTPS", status_code = "HTTP_301". No ssl_policy or certificate_arn on HTTP listener (unsupported argument error if added). No tags block on listeners.

    3. aws_lb_listener with label "https": load_balancer_arn = aws_lb.main.arn, port = "443", protocol = "HTTPS", ssl_policy = "ELBSecurityPolicy-TLS13-1-2-2021-06", certificate_arn = var.acm_cert_arn. Default action type = "fixed-response" with nested fixed_response block: content_type = "text/plain", message_body = "No tenant provisioned", status_code = "503". No tags block on listeners.

    variables.tf: Keep existing name_prefix variable. Add three more required variables (no defaults):
    - acm_cert_arn: type = string, description = "ARN of the wildcard ACM certificate for the HTTPS:443 listener."
    - subnet_ids: type = list(string), description = "Public subnet IDs for the ALB (across >=2 AZs). Sourced from module.networking.private_subnet_ids."
    - security_group_id: type = string, description = "ALB security group ID (allows ingress 80/443 from internet). Sourced from module.networking.alb_security_group_id."

    outputs.tf: Keep the header comment. Add output "listener_arn" with description "HTTPS:443 listener ARN -> provisioner `aws_alb_listener_arn`." and value = aws_lb_listener.https.arn.
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform/modules/alb && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - modules/alb/main.tf contains resource "aws_lb" "main" with idle_timeout = 120, enable_http2 = true, drop_invalid_header_fields = true, enable_deletion_protection = false, internal = false, load_balancer_type = "application"
    - modules/alb/main.tf does NOT contain idle_timeout on any aws_lb_listener resource
    - modules/alb/main.tf contains resource "aws_lb_listener" "http" with protocol = "HTTP" and redirect block with status_code = "HTTP_301"
    - modules/alb/main.tf does NOT contain ssl_policy on the "http" listener
    - modules/alb/main.tf contains resource "aws_lb_listener" "https" with protocol = "HTTPS", ssl_policy = "ELBSecurityPolicy-TLS13-1-2-2021-06", certificate_arn = var.acm_cert_arn, and fixed_response block with status_code = "503"
    - modules/alb/main.tf does NOT contain aws_alb resource type (use aws_lb only)
    - modules/alb/variables.tf contains variables: name_prefix, acm_cert_arn, subnet_ids (list(string)), security_group_id
    - modules/alb/outputs.tf contains output "listener_arn" with value = aws_lb_listener.https.arn
    - terraform validate passes in modules/alb/ directory
  </acceptance_criteria>
  <done>modules/alb fully implemented: internet-facing ALB with idle_timeout = 120, HTTP:80 redirect listener, HTTPS:443 fixed-response 503 listener with ACM cert and TLS 1.2/1.3 policy, four variables declared, listener_arn output. terraform validate passes in modules/alb/.</done>
</task>

<task type="auto">
  <name>Task 2: Update Makefile plan-check with tenant_domain dummy var injection</name>
  <files>Makefile</files>
  <read_first>
    Makefile (current plan-check target — find the terraform plan -input=false line at line 48)
    .planning/phases/05-tls-and-routing/05-RESEARCH.md (Pitfall 1 section: exact change required and rationale)
    .planning/phases/05-tls-and-routing/05-PATTERNS.md (Makefile section: current line and required replacement)
  </read_first>
  <action>
    In the Makefile, find the terraform plan command inside the plan-check target (currently "terraform plan -input=false" at the end of the chained && commands). Add -var "tenant_domain=placeholder.example.com" to that command so it becomes:

      terraform plan -input=false -var "tenant_domain=placeholder.example.com"

    This is a one-line change. The dummy domain placeholder.example.com satisfies the regex ^[a-z0-9][a-z0-9.-]+\.[a-z]{2,}$ (non-empty, valid domain shape, plainly not a real domain). This must be committed in the Makefile because the gitignored terraform.tfvars is not present during offline plan-check runs. Do not change any other part of the Makefile.
  </action>
  <verify>
    <automated>grep -c 'tenant_domain=placeholder.example.com' /home/yevhenii/projects/saas/platform-terraform/Makefile</automated>
  </verify>
  <acceptance_criteria>
    - Makefile plan-check target contains "tenant_domain=placeholder.example.com" in the terraform plan command line
    - The terraform plan line reads: terraform plan -input=false -var "tenant_domain=placeholder.example.com"
    - No other lines in Makefile are changed
    - grep -c 'tenant_domain=placeholder.example.com' Makefile returns 1
  </acceptance_criteria>
  <done>Makefile plan-check target updated with dummy tenant_domain var injection. The offline gate will pass the regex validation in modules/acm and modules/route53 without requiring a real terraform.tfvars file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → ALB security group | Public ingress on 80/443; ALB SG is Phase 1 (no change this phase) |
| ALB → task security group | Port 8069 only from ALB SG id; Phase 1 (no change this phase) |
| var.acm_cert_arn → HTTPS listener | String input; ACM ARN flows from module.acm.cert_arn at wiring time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Info Disclosure | HTTP plaintext client traffic | mitigate | aws_lb_listener.http issues 301 redirect to HTTPS:443 (per D-01); all cleartext traffic is upgraded before reaching any backend |
| T-05-06 | Tampering | Weak TLS ciphers on HTTPS listener | mitigate | ssl_policy = "ELBSecurityPolicy-TLS13-1-2-2021-06" enforces TLS 1.2 minimum and supports TLS 1.3; satisfies ASVS V6 Cryptography L1 |
| T-05-07 | Tampering | Invalid/malicious HTTP headers reaching tasks | mitigate | drop_invalid_header_fields = true on aws_lb.main strips headers that don't conform to HTTP/1.1 spec (per D-04 free hardening) |
| T-05-08 | Elevation | Direct internet access bypassing ALB | accept | Task SG accepts 8069 only from ALB SG id (Phase 1, unchanged); this plan does not weaken that boundary. Accepted as already-mitigated. |
| T-05-09 | Denial of Service | ALB deletion_protection = false | accept | enable_deletion_protection = false per D-04 cost-lean posture; Terraform lifecycle + code review is the guard; accidental destroy recoverable from IaC. Low risk for plan-only milestone. |
| T-05-10 | Info Disclosure | Makefile dummy var tenant_domain=placeholder.example.com committed | accept | Not a secret; a plainly non-real domain committed to Makefile for the offline gate. No credentials or sensitive data. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all resources use the already-pinned hashicorp/aws 6.51.0 provider |
</threat_model>

<verification>
After both tasks complete:

1. terraform validate in modules/alb/ passes
2. grep -c 'tenant_domain=placeholder.example.com' Makefile returns 1
3. terraform fmt -check -recursive passes (run from repo root)

Full make plan-check gate is verified in plan 05-03 after wiring.
</verification>

<success_criteria>
- modules/alb/main.tf: aws_lb.main (internet-facing, idle_timeout=120, drop_invalid_header_fields=true, enable_http2=true, enable_deletion_protection=false) + aws_lb_listener.http (301 redirect) + aws_lb_listener.https (ELBSecurityPolicy-TLS13-1-2-2021-06, fixed-response 503)
- modules/alb/variables.tf: name_prefix, acm_cert_arn, subnet_ids (list(string)), security_group_id — all required, no defaults
- modules/alb/outputs.tf: listener_arn = aws_lb_listener.https.arn with provisioner contract description
- Makefile: plan-check target runs terraform plan -input=false -var "tenant_domain=placeholder.example.com"
- terraform fmt -check -recursive passes
- terraform validate passes in modules/alb/
</success_criteria>

<output>
Create `.planning/phases/05-tls-and-routing/05-02-SUMMARY.md` when done.
</output>
