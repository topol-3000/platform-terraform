---
phase: 01-networking-module
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - envs/prod/variables.tf
  - envs/prod/main.tf
  - envs/prod/outputs.tf
  - Makefile
autonomous: true
requirements: [NET-06, NET-07]
must_haves:
  truths:
    - "envs/prod declares vpc_cidr and azs root variables with prod-sensible defaults (D-11)."
    - "The module \"networking\" call in envs/prod/main.tf is uncommented and passes name_prefix, vpc_cidr, azs."
    - "envs/prod/outputs.tf re-exports private_subnet_ids, task_security_group_id, vpc_id, alb_security_group_id from module.networking."
    - "make plan runs offline via inline dummy AWS credentials, and providers.tf stays clean with no skip_* flags (D-06, D-07)."
    - "terraform fmt -check (recursive), terraform validate, and terraform plan in envs/prod all pass and the plan is non-empty with networking resources and no NAT gateway."
  artifacts:
    - path: "envs/prod/main.tf"
      provides: "uncommented module \"networking\" call wired with root variables"
      contains: "module \"networking\""
    - path: "envs/prod/outputs.tf"
      provides: "four re-exported networking outputs"
      contains: "module.networking.private_subnet_ids"
    - path: "envs/prod/variables.tf"
      provides: "vpc_cidr and azs root passthrough variables with defaults"
      contains: "variable \"vpc_cidr\""
    - path: "Makefile"
      provides: "plan target with inline dummy AWS env"
      contains: "AWS_ACCESS_KEY_ID=dummy"
  key_links:
    - from: "envs/prod/main.tf module.networking"
      to: "modules/networking"
      via: "source + name_prefix/vpc_cidr/azs inputs"
      pattern: "source\\s*=\\s*\"../../modules/networking\""
    - from: "envs/prod/outputs.tf"
      to: "module.networking outputs"
      via: "re-export"
      pattern: "module\\.networking\\.(private_subnet_ids|task_security_group_id|vpc_id|alb_security_group_id)"
    - from: "Makefile plan target"
      to: "terraform plan"
      via: "inline dummy AWS credentials"
      pattern: "AWS_ACCESS_KEY_ID=dummy"
---

<objective>
Wire `modules/networking` into the prod root config and prove the code-complete gate: add the `vpc_cidr`/`azs` root variables, uncomment and extend the `module "networking"` call, uncomment/add the four re-exported outputs, give the Makefile `plan` target inline dummy AWS credentials so it runs offline, then verify `terraform fmt -check`, `terraform validate`, and a non-empty `terraform plan`.

Purpose: NET-06 + NET-07 — without wiring, the module produces no plan; the dummy-env Makefile change is the concrete offline verification gate (D-06) while providers.tf stays clean for real applies (D-07).
Output: Updated envs/prod/{variables,main,outputs}.tf and Makefile; a passing fmt/validate and a non-empty plan recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-networking-module/01-CONTEXT.md
@.planning/phases/01-networking-module/01-PATTERNS.md
@CLAUDE.md
@.planning/phases/01-networking-module/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (modules/networking), these outputs now exist and MUST be re-exported by name: -->
<!--   module.networking.private_subnet_ids       (public subnets, under the contract name) -->
<!--   module.networking.task_security_group_id -->
<!--   module.networking.vpc_id -->
<!--   module.networking.alb_security_group_id -->
<!-- Module inputs to pass: name_prefix (string), vpc_cidr (string), azs (list(string)). -->

Existing in envs/prod/main.tf (DO NOT redefine):
  locals { name_prefix = "${var.project}-${var.environment}" }   # = "odoo-saas-prod"

Existing commented block to uncomment (envs/prod/main.tf lines 13-17):
  # module "networking" { source = "../../modules/networking"; name_prefix = local.name_prefix }

Existing commented re-exports to uncomment (envs/prod/outputs.tf):
  # output "private_subnet_ids"   { value = module.networking.private_subnet_ids }
  # output "task_security_group_id" { value = module.networking.task_security_group_id }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root vpc_cidr/azs variables and wire the module call</name>
  <read_first>
    - envs/prod/variables.tf (the file being modified — region/environment/project/tenant_domain analogs)
    - envs/prod/main.tf (the file being modified — existing locals.name_prefix and commented module "networking" block lines 13-17)
    - .planning/phases/01-networking-module/01-PATTERNS.md (sections "envs/prod/main.tf" and "envs/prod/variables.tf")
    - .planning/phases/01-networking-module/01-01-SUMMARY.md (confirm the module input/output names Plan 01 produced)
  </read_first>
  <files>envs/prod/variables.tf, envs/prod/main.tf</files>
  <action>
    In envs/prod/variables.tf add two root passthrough variables with prod-sensible defaults so `make plan` runs with zero terraform.tfvars editing (D-11): `vpc_cidr` (type string, default "10.0.0.0/16", period-terminated description) and `azs` (type list(string), default ["eu-central-1a", "eu-central-1b"], period-terminated description). Match the simple defaulted form of the existing `region` variable.
    In envs/prod/main.tf uncomment the `module "networking"` block (currently lines 13-17) and extend it: keep `source = "../../modules/networking"` and `name_prefix = local.name_prefix`, add `vpc_cidr = var.vpc_cidr` and `azs = var.azs`. Do NOT redefine locals.name_prefix (already present). Leave all downstream commented module blocks (ecr/ecs/rds/efs/acm/alb/route53/ssm) commented out. The module calls no other module — all wiring stays here.
  </action>
  <verify>
    <automated>grep -q 'variable "vpc_cidr"' envs/prod/variables.tf && grep -q 'variable "azs"' envs/prod/variables.tf && grep -Eq '^\s*module "networking"' envs/prod/main.tf && grep -Eq '^\s*vpc_cidr\s*=\s*var.vpc_cidr' envs/prod/main.tf && grep -Eq '^\s*azs\s*=\s*var.azs' envs/prod/main.tf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - envs/prod/variables.tf declares vpc_cidr (default "10.0.0.0/16") and azs (list(string), default ["eu-central-1a","eu-central-1b"]).
    - The `module "networking"` block is uncommented (a non-comment line `module "networking" {` exists) and passes source, name_prefix = local.name_prefix, vpc_cidr = var.vpc_cidr, azs = var.azs.
    - No second `locals { name_prefix }` was added.
    - Downstream module blocks (ecr/ecs/rds/...) remain commented.
  </acceptance_criteria>
  <done>The prod root declares vpc_cidr/azs and the networking module is wired with name_prefix, vpc_cidr, and azs.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export the four networking outputs and add offline dummy-AWS env to make plan</name>
  <read_first>
    - envs/prod/outputs.tf (the file being modified — commented re-exports for private_subnet_ids and task_security_group_id, plus the `-> provisioner` description convention)
    - Makefile (the file being modified — existing plan target lines 19-20; recipes use TAB indentation)
    - .planning/phases/01-networking-module/01-PATTERNS.md (sections "envs/prod/outputs.tf" and "Makefile")
    - envs/prod/providers.tf (confirm it stays clean — D-07, no skip_* flags added)
  </read_first>
  <files>envs/prod/outputs.tf, Makefile</files>
  <action>
    In envs/prod/outputs.tf uncomment the `private_subnet_ids` and `task_security_group_id` re-exports (keep the `-> provisioner \`<setting>\`` description mapping). Add two NEW outputs following the same form: `vpc_id` (value = module.networking.vpc_id) and `alb_security_group_id` (value = module.networking.alb_security_group_id), with descriptions noting they are consumed by downstream rds/ecs/alb modules (no provisioner-contract mapping yet). Leave outputs for unbuilt modules (ecs/alb/rds/efs/acm/route53/ecr) commented out.
    In the Makefile, modify ONLY the `plan` target recipe (currently `cd $(ENV_DIR) && terraform plan`) to prefix terraform plan with inline dummy AWS env: AWS_ACCESS_KEY_ID=dummy AWS_SECRET_ACCESS_KEY=dummy AWS_REGION=eu-central-1 (D-06). Keep TAB indentation on the recipe line. Leave init/apply/destroy/fmt/validate targets unchanged. Do NOT add skip_credentials_validation / skip_requesting_account_id / skip_metadata_api_check to envs/prod/providers.tf (D-07) — the dummy-env approach lives only in the Makefile.
  </action>
  <verify>
    <automated>grep -Eq '^output "private_subnet_ids"' envs/prod/outputs.tf && grep -Eq '^output "task_security_group_id"' envs/prod/outputs.tf && grep -Eq '^output "vpc_id"' envs/prod/outputs.tf && grep -Eq '^output "alb_security_group_id"' envs/prod/outputs.tf && grep -q 'AWS_ACCESS_KEY_ID=dummy' Makefile && ! grep -q 'skip_credentials_validation' envs/prod/providers.tf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - envs/prod/outputs.tf has four uncommented outputs: private_subnet_ids, task_security_group_id, vpc_id, alb_security_group_id — each sourcing module.networking.<name>.
    - private_subnet_ids and task_security_group_id keep the `-> provisioner` mapping in their descriptions.
    - The Makefile `plan` recipe includes `AWS_ACCESS_KEY_ID=dummy AWS_SECRET_ACCESS_KEY=dummy AWS_REGION=eu-central-1` before `terraform plan`, indented with a TAB.
    - envs/prod/providers.tf contains no skip_credentials_validation / skip_requesting_account_id / skip_metadata_api_check.
  </acceptance_criteria>
  <done>The four networking outputs are re-exported and `make plan` carries inline dummy AWS credentials while providers.tf stays clean.</done>
</task>

<task type="auto">
  <name>Task 3: Run the code-complete verification gate (fmt-check, validate, non-empty plan)</name>
  <read_first>
    - Makefile (the fmt/validate/plan targets being exercised)
    - .planning/ROADMAP.md (Phase 1 Success Criteria — the five conditions this gate proves)
    - CLAUDE.md (Verification constraint: code-complete only — fmt -check, validate, non-empty plan; no apply)
  </read_first>
  <files>(no source files modified — verification + fixups only)</files>
  <action>
    Ensure Terraform >= 1.11 is available (it is NOT on PATH in this environment — install it or use a local install; do NOT downgrade the required_version). Run, from envs/prod: `terraform init` (offline provider resolution may require network for the registry — if the executor lacks network, document this in the SUMMARY and rely on the source-level grep gates from Plan 01 + Tasks 1-2 as the completion evidence). Then run `terraform fmt -check -recursive` at repo root, `terraform validate` in envs/prod, and `make plan` (offline dummy env). If fmt -check fails, run `terraform fmt -recursive` and re-check. If validate or plan surfaces an error, fix the offending source (commonly an output splat mismatch or a missing module input) and re-run — do NOT relax the locked decisions to make it pass. Capture the plan summary line (resources to add) in the SUMMARY. The plan MUST be non-empty and MUST NOT contain aws_nat_gateway.
  </action>
  <verify>
    <automated>cd envs/prod && terraform fmt -check -recursive ../.. && terraform validate && AWS_ACCESS_KEY_ID=dummy AWS_SECRET_ACCESS_KEY=dummy AWS_REGION=eu-central-1 terraform plan -no-color | tee /tmp/np-plan.txt | grep -Eq 'aws_vpc.main' && grep -q 'aws_internet_gateway' /tmp/np-plan.txt && grep -q 'aws_subnet.public' /tmp/np-plan.txt && grep -Eq 'aws_security_group.(alb|task)' /tmp/np-plan.txt && ! grep -q 'aws_nat_gateway' /tmp/np-plan.txt && echo PLAN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `terraform fmt -check -recursive` exits 0 (repo formatted).
    - `terraform validate` in envs/prod exits 0.
    - `make plan` (offline dummy env) produces a NON-EMPTY plan whose output includes aws_vpc.main, aws_subnet.public (one per AZ), aws_internet_gateway, an aws_route_table with a 0.0.0.0/0 → IGW route, and aws_security_group for both alb and task.
    - The plan output contains NO aws_nat_gateway.
    - The task SG's 8069 ingress in the plan shows the ALB SG id as source (security_groups), not a CIDR.
    - If terraform/network is genuinely unavailable, the SUMMARY documents this explicitly and records the passing source-level grep gates as the fallback evidence (per CLAUDE.md code-complete scope).
  </acceptance_criteria>
  <done>fmt -check, validate, and a non-empty NAT-free terraform plan all pass (or the environment limitation is documented with source-gate fallback evidence).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Makefile env → terraform/AWS provider | Dummy credentials injected only for offline plan; no real AWS auth crosses here. |
| envs/prod/outputs.tf → provisioner | Resource ids re-exported as the typed provisioner contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | envs/prod/outputs.tf re-exports | mitigate | Re-exported outputs are resource ids only (vpc/subnet/SG ids) — no secrets or credentials in outputs/state (CLAUDE.md anti-pattern guard); asserted in Task 2. |
| T-02-02 | Spoofing / Credential misuse | Makefile dummy AWS creds | accept | `dummy`/`dummy` are non-functional placeholders for an offline plan with no data sources or state refresh; no real credentials are committed, and providers.tf stays clean for real applies (D-07). |
| T-02-03 | Tampering | terraform init provider download | mitigate | Provider is the registry-resolved hashicorp/aws pinned `~> 6.0` in envs/prod/versions.tf with required_version >= 1.11; no new package-manager installs introduced by this plan. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan; Terraform provider resolution only, version-constrained. |
</threat_model>

<verification>
The end-to-end Phase 1 gate runs here (Task 3): `terraform fmt -check -recursive`, `terraform validate` in envs/prod, and a non-empty offline `terraform plan` that shows the VPC/subnets/IGW/route-table/SGs and no NAT gateway.

Environment note: terraform is NOT on PATH in this workspace and `terraform init` may require registry network access. If unavailable, the executor must install Terraform >= 1.11 / obtain network, OR document the limitation and fall back to the source-level grep gates (which fully assert the locked decisions) per CLAUDE.md's code-complete scope.
</verification>

<success_criteria>
- envs/prod declares vpc_cidr and azs with prod defaults.
- The networking module is wired (name_prefix, vpc_cidr, azs) and its four outputs re-exported (private_subnet_ids, task_security_group_id, vpc_id, alb_security_group_id).
- `make plan` carries inline dummy AWS env; providers.tf has no skip_* flags.
- fmt -check + validate pass; terraform plan is non-empty, NAT-free, and shows the expected networking resources (or the environment limitation is documented with source-gate fallback).
</success_criteria>

<output>
Create `.planning/phases/01-networking-module/01-02-SUMMARY.md` when done.
</output>
