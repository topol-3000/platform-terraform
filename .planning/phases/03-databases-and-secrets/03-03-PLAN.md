---
phase: 03-databases-and-secrets
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - modules/rds-control-plane/variables.tf
  - modules/rds-control-plane/main.tf
  - modules/rds-control-plane/outputs.tf
autonomous: true
requirements:
  - RDS-03
must_haves:
  truths:
    - "modules/rds-control-plane/variables.tf declares all required variables: name_prefix, subnet_ids, vpc_id, task_security_group_id, master_password (sensitive), instance_class, engine_version, allocated_storage, max_allocated_storage"
    - "modules/rds-control-plane/main.tf declares aws_security_group.cp_rds with ingress on port 5432 using security_groups = [var.task_security_group_id] — no cidr_blocks on the 5432 ingress rule"
    - "modules/rds-control-plane/main.tf declares a Multi-AZ aws_db_instance.control_plane with multi_az = true, storage_encrypted = true, deletion_protection = true, skip_final_snapshot = false, and lifecycle { ignore_changes = [password] }"
    - "modules/rds-control-plane resources share NO names, subnet groups, SGs, or identifiers with modules/rds-tenant"
    - "No data sources anywhere in modules/rds-control-plane/main.tf"
  artifacts:
    - path: "modules/rds-control-plane/variables.tf"
      provides: "All required variable declarations (D-11 pre-declaration requirement)"
      contains: "variable \"vpc_id\""
    - path: "modules/rds-control-plane/main.tf"
      provides: "Separate SG + subnet group + parameter group + Multi-AZ RDS instance"
      contains: "multi_az = true"
    - path: "modules/rds-control-plane/outputs.tf"
      provides: "endpoint output"
      contains: "output \"endpoint\""
  key_links:
    - from: "modules/rds-control-plane/main.tf"
      to: "var.task_security_group_id"
      via: "aws_security_group.cp_rds ingress block"
      pattern: "security_groups\\s*=\\s*\\[var\\.task_security_group_id\\]"
    - from: "modules/rds-control-plane/main.tf"
      to: "var.master_password"
      via: "aws_db_instance.control_plane.password"
      pattern: "password\\s*=\\s*var\\.master_password"
---

<objective>
Implement modules/rds-control-plane: pre-declare all required variables (D-11), then implement the separate Multi-AZ PostgreSQL instance for provisioner control-plane data — structurally identical to rds-tenant but with multi_az = true and fully isolated resources (separate SG, subnet group, parameter group, identifiers).

Purpose: Control-plane RDS holds provisioner operational data (not tenant data). SEED-001 requires strict isolation — no shared resources with rds-tenant. Multi-AZ provides 99.9% SLA for the provisioner's operational database. This plan runs in Wave 2 parallel with Plan 02 (rds-tenant) — zero file overlap.

Output: modules/rds-control-plane/{variables,main,outputs}.tf fully implemented. The endpoint output feeds Plan 05 (envs/prod wiring).
</objective>

<execution_context>
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-CONTEXT.md
@/home/yevhenii/.claude/get-shit-done/workflows/execute-plan.md
@/home/yevhenii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/yevhenii/projects/saas/platform-terraform/.planning/ROADMAP.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-RESEARCH.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-PATTERNS.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-01-SUMMARY.md

<interfaces>
<!-- Primary analog: modules/rds-tenant (Plan 02) — same structure, multi_az=true is the key difference -->
<!-- Secondary analog: modules/networking/main.tf lines 86-108 — SG-reference ingress template -->

Key differences from rds-tenant (all resource labels/names use cp/control_plane suffix):
  - resource label: aws_security_group.cp_rds (NOT rds_tenant)
  - name: "${var.name_prefix}-cp-rds-sg"
  - resource label: aws_db_subnet_group.cp (NOT tenant)
  - name: "${var.name_prefix}-cp-rds"
  - resource label: aws_db_parameter_group.cp (NOT tenant)
  - name_prefix: "${var.name_prefix}-cp-pg16-"
  - resource label: aws_db_instance.control_plane (NOT tenant)
  - identifier: "${var.name_prefix}-cp-rds"
  - db_name: "provisioner" (NOT "odoo_shared" — this is the provisioner's own database)
  - multi_az = true (NOT false)
  - final_snapshot_identifier: "${var.name_prefix}-cp-rds-final"

From modules/ssm/outputs.tf (Plan 01 — what rds-control-plane receives as master_password):
  output "cp_rds_password" { sensitive = true; value = random_password.cp_rds.result }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-declare all variables in modules/rds-control-plane/variables.tf (D-11)</name>
  <files>modules/rds-control-plane/variables.tf</files>

  <read_first>
    - modules/rds-control-plane/variables.tf (current stub — only name_prefix declared)
    - .planning/phases/03-databases-and-secrets/03-PATTERNS.md §modules/rds-control-plane/variables.tf and §modules/rds-control-plane/outputs.tf
    - .planning/phases/03-databases-and-secrets/03-RESEARCH.md §Pitfall 1: Undeclared Variables Cause Plan Failure
    - modules/rds-tenant/variables.tf (just written in Plan 02 — exact same variable set)
  </read_first>

  <action>
  Replace modules/rds-control-plane/variables.tf with the identical variable set as modules/rds-tenant/variables.tf — the control-plane module accepts the same inputs (name_prefix, subnet_ids, vpc_id, task_security_group_id, master_password, instance_class, engine_version, allocated_storage, max_allocated_storage) and the same defaults.

  Required variables (no default):
    - name_prefix: description "Prefix for resource names, e.g. \"odoo-saas-prod\".", type = string
    - subnet_ids: description "Subnet IDs for the control-plane RDS DB subnet group. Requires >=2 subnets in different AZs.", type = list(string)
    - vpc_id: description "VPC id for the control-plane RDS security group.", type = string
    - task_security_group_id: description "Security group id for ECS tasks. Control-plane RDS SG allows 5432 ingress from this SG only.", type = string
    - master_password: description "Control-plane RDS master password (sensitive). Sourced from modules/ssm random_password output.", type = string, sensitive = true

  Optional variables (same prod-sensible defaults as rds-tenant):
    - instance_class: type = string, default = "db.t4g.small", description "RDS instance class for the control-plane PostgreSQL instance."
    - engine_version: type = string, default = "16", description "PostgreSQL engine version for the control-plane instance."
    - allocated_storage: type = number, default = 20, description "Initial allocated storage in GiB."
    - max_allocated_storage: type = number, default = 100, description "Maximum allocated storage for autoscaling in GiB."

  All descriptions end with a period. No validation blocks.
  </action>

  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep "variable" modules/rds-control-plane/variables.tf | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - modules/rds-control-plane/variables.tf declares 9 variables: name_prefix, subnet_ids, vpc_id, task_security_group_id, master_password, instance_class, engine_version, allocated_storage, max_allocated_storage
    - master_password has sensitive = true
    - subnet_ids has type = list(string)
    - All optional variables have the same defaults as rds-tenant (instance_class = "db.t4g.small", engine_version = "16", allocated_storage = 20, max_allocated_storage = 100)
    - All description strings end with a period
  </acceptance_criteria>

  <done>modules/rds-control-plane/variables.tf has 9 variables matching the rds-tenant module interface; master_password is sensitive.</done>
</task>

<task type="auto">
  <name>Task 2: Implement modules/rds-control-plane/main.tf and modules/rds-control-plane/outputs.tf</name>
  <files>modules/rds-control-plane/main.tf, modules/rds-control-plane/outputs.tf</files>

  <read_first>
    - modules/rds-control-plane/main.tf (current stub — header only)
    - modules/rds-control-plane/outputs.tf (current stub — header comment only)
    - modules/rds-control-plane/variables.tf (just written in Task 1)
    - modules/rds-tenant/main.tf (Plan 02 — the structural analog; adapt with cp/control_plane labels and multi_az = true)
    - .planning/phases/03-databases-and-secrets/03-PATTERNS.md §modules/rds-control-plane/main.tf
    - .planning/phases/03-databases-and-secrets/03-RESEARCH.md §Pattern 2 (aws_db_instance) + §Offline Plan Landmines
    - modules/networking/main.tf lines 86-108 (SG-reference ingress template)
    - bootstrap/main.tf (lifecycle WHY-comment analog)
  </read_first>

  <action>
  Replace modules/rds-control-plane/main.tf: update header comment removing STATUS stub/TODO, keep the SEED-001 isolation note ("MUST be separate from the tenant RDS. Never mix tenant + platform data. multi_az = true for 99.9% SLA on provisioner operational data."), then implement resources in this order:

  1. aws_security_group.cp_rds — same SG-reference ingress pattern as rds-tenant; only labels/names differ:
     name_prefix = "${var.name_prefix}-cp-rds-", vpc_id = var.vpc_id
     ingress: description = "PostgreSQL from task SG only", from_port = 5432, to_port = 5432, protocol = "tcp", security_groups = [var.task_security_group_id] (NOT cidr_blocks)
     egress: from_port = 0, to_port = 0, protocol = "-1", cidr_blocks = ["0.0.0.0/0"]
     tags = { Name = "${var.name_prefix}-cp-rds-sg" }
     WHY-comment: "Provisioner connects to the control-plane DB from ECS tasks (5432 from task SG, SG reference not CIDR — mirrors the tenant RDS pattern)."

  2. aws_db_subnet_group.cp:
     name = "${var.name_prefix}-cp-rds", description = "Subnet group for the control-plane RDS instance.", subnet_ids = var.subnet_ids
     tags = { Name = "${var.name_prefix}-cp-rds-subnet-group" }
     WHY-comment: "Separate subnet group from the tenant RDS — no shared resources between tenant and control-plane (SEED-001 isolation)."

  3. aws_db_parameter_group.cp:
     name_prefix = "${var.name_prefix}-cp-pg16-", family = "postgres16", description = "Parameter group for the control-plane PostgreSQL 16 instance."
     lifecycle { create_before_destroy = true }
     tags = { Name = "${var.name_prefix}-cp-pg16" }

  4. aws_db_instance.control_plane — identical structure to rds-tenant with these differences:
     identifier = "${var.name_prefix}-cp-rds"
     engine = "postgres", engine_version = var.engine_version, parameter_group_name = aws_db_parameter_group.cp.name
     instance_class = var.instance_class
     allocated_storage = var.allocated_storage, max_allocated_storage = var.max_allocated_storage, storage_type = "gp3", storage_encrypted = true
     db_name = "provisioner", username = "odoo_master", password = var.master_password
     port = 5432
     db_subnet_group_name = aws_db_subnet_group.cp.name, vpc_security_group_ids = [aws_security_group.cp_rds.id]
     publicly_accessible = false
     multi_az = true (D-07 / Multi-AZ for 99.9% SLA on provisioner operational data — the key difference from rds-tenant)
     deletion_protection = true, skip_final_snapshot = false, final_snapshot_identifier = "${var.name_prefix}-cp-rds-final" (D-08)
     backup_retention_period = 7, copy_tags_to_snapshot = true
     tags = { Name = "${var.name_prefix}-cp-rds" }
     lifecycle { ignore_changes = [password] } (D-02)
     WHY-comment: "Control-plane data must never be accidentally destroyed — this is the provisioner's operational database. deletion_protection + final snapshot mirror the bootstrap S3 ethos. multi_az = true for 99.9% SLA."

  PROHIBITED (same as rds-tenant): manage_master_user_password = true, any data source blocks, cidr_blocks on the 5432 ingress rule.

  Replace modules/rds-control-plane/outputs.tf — the provisioner contract needs only the endpoint (no identifier or security_group_id pass-through needed for rds-proxy, which only fronts the tenant instance):
    - output "endpoint": description "Control-plane RDS endpoint -> provisioner `aws_control_plane_rds_endpoint`.", value = aws_db_instance.control_plane.endpoint
    - output "security_group_id": description "Control-plane RDS security group id.", value = aws_security_group.cp_rds.id

  All descriptions end with period.
  </action>

  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep -c "multi_az" modules/rds-control-plane/main.tf && grep -c "storage_encrypted" modules/rds-control-plane/main.tf && grep -c "deletion_protection" modules/rds-control-plane/main.tf && grep "multi_az" modules/rds-control-plane/main.tf</automated>
  </verify>

  <acceptance_criteria>
    - modules/rds-control-plane/main.tf contains multi_az = true (NOT false)
    - modules/rds-control-plane/main.tf contains storage_encrypted = true
    - modules/rds-control-plane/main.tf contains deletion_protection = true
    - modules/rds-control-plane/main.tf contains skip_final_snapshot = false
    - modules/rds-control-plane/main.tf contains security_groups = [var.task_security_group_id] for the 5432 ingress (no cidr_blocks on this rule)
    - modules/rds-control-plane/main.tf contains lifecycle { ignore_changes = [password] }
    - modules/rds-control-plane/main.tf has NO data source blocks
    - modules/rds-control-plane/main.tf db_name = "provisioner" (NOT "odoo_shared")
    - modules/rds-control-plane/main.tf resource labels all use cp or control_plane suffix (no "tenant" label appears)
    - modules/rds-control-plane/outputs.tf contains output "endpoint" referencing aws_db_instance.control_plane.endpoint
    - No resource name in rds-control-plane/main.tf duplicates any name from rds-tenant/main.tf (identifiers, subnet group names, SG name_prefix, param group name_prefix all differ)
  </acceptance_criteria>

  <done>modules/rds-control-plane/main.tf has 4 fully isolated resources (cp_rds SG, cp subnet group, cp parameter group, Multi-AZ control_plane db_instance) with multi_az = true and no overlap with rds-tenant. outputs.tf exports endpoint and security_group_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| var.master_password → aws_db_instance.control_plane | Sensitive password (from ssm module) crosses into RDS instance password — stored encrypted in S3 state |
| rds-tenant vs rds-control-plane isolation | These two db_instance resources must share NO subnet groups, SGs, or identifiers — blast-radius isolation per SEED-001 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | modules/rds-control-plane/main.tf — master_password in state | mitigate | password = var.master_password (sensitive); lifecycle { ignore_changes = [password] }; state bucket encrypted AES-256 SSE |
| T-03-09 | Elevation of Privilege | aws_security_group.cp_rds ingress | mitigate | ingress uses security_groups = [var.task_security_group_id]; no cidr_blocks on 5432 rule; provisioner accesses DB only through task SG |
| T-03-10 | Tampering | Tenant/control-plane isolation | mitigate | All cp_rds resources use distinct names/identifiers from rds-tenant; verified by acceptance criteria (no "tenant" label in cp module, db_name = "provisioner" not "odoo_shared") |
| T-03-11 | Tampering | deletion_protection on control-plane | mitigate | deletion_protection = true + skip_final_snapshot = false; two independent guards against data loss on the provisioner's operational DB |
</threat_model>

<verification>
After both tasks complete:
- modules/rds-control-plane/variables.tf: 9 variables matching rds-tenant interface
- modules/rds-control-plane/main.tf: multi_az = true; SG uses security_groups = [var.task_security_group_id]; db_name = "provisioner"; all resource labels use cp/control_plane suffix; no shared names with rds-tenant; no data sources
- modules/rds-control-plane/outputs.tf: endpoint + security_group_id
- terraform fmt -check passes on modules/rds-control-plane/
</verification>

<success_criteria>
modules/rds-control-plane fully implemented: Multi-AZ PostgreSQL instance with strict isolation from rds-tenant (separate SG, subnet group, parameter group, identifier, db_name = "provisioner"), SG-reference port-5432 ingress from task SG only, deletion protection + final snapshot, storage encryption. Endpoint output exported. No data sources.
</success_criteria>

<output>
Create .planning/phases/03-databases-and-secrets/03-03-SUMMARY.md when done.
</output>
