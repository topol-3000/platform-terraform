---
phase: 03-databases-and-secrets
plan: 05
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - envs/prod/versions.tf
  - envs/prod/variables.tf
  - envs/prod/main.tf
  - envs/prod/outputs.tf
autonomous: true
requirements:
  - RDS-04
  - SSM-02
must_haves:
  truths:
    - "envs/prod/versions.tf required_providers includes hashicorp/random ~> 3.0"
    - "envs/prod/variables.tf declares enable_rds_proxy (bool, default false) and the four rds_* sizing variables with prod-sensible defaults"
    - "envs/prod/main.tf module calls for ssm, rds_tenant, rds_proxy, and rds_control_plane are uncommented with full wiring (underscore labels, correct input references)"
    - "envs/prod/outputs.tf tenant_rds_endpoint, rds_proxy_endpoint, and control_plane_rds_endpoint outputs are active and resolve"
    - "make plan-check exits 0: terraform fmt -check passes, terraform validate passes, and the plan is non-empty showing both RDS instances and 3 SSM parameters"
    - "With enable_rds_proxy default false, terraform plan shows 0 aws_db_proxy resources"
  artifacts:
    - path: "envs/prod/versions.tf"
      provides: "hashicorp/random ~> 3.0 in required_providers"
      contains: "hashicorp/random"
    - path: "envs/prod/variables.tf"
      provides: "enable_rds_proxy + rds sizing variables"
      contains: "enable_rds_proxy"
    - path: "envs/prod/main.tf"
      provides: "4 new module calls uncommented and wired"
      contains: "module \"ssm\""
    - path: "envs/prod/outputs.tf"
      provides: "tenant_rds_endpoint, rds_proxy_endpoint, control_plane_rds_endpoint active"
      contains: "tenant_rds_endpoint"
  key_links:
    - from: "envs/prod/main.tf module.ssm"
      to: "modules/ssm outputs"
      via: "module.ssm.tenant_rds_password → module.rds_tenant.master_password"
      pattern: "module\\.ssm\\.tenant_rds_password"
    - from: "envs/prod/main.tf module.rds_proxy"
      to: "module.rds_tenant"
      via: "db_instance_identifier = module.rds_tenant.identifier"
      pattern: "module\\.rds_tenant\\.identifier"
    - from: "envs/prod/outputs.tf"
      to: "module.rds_proxy.endpoint"
      via: "try() guarded output"
      pattern: "module\\.rds_proxy\\.endpoint"
---

<objective>
Wire all four Phase 3 modules into envs/prod: add hashicorp/random to required_providers (L-5 landmine fix), add enable_rds_proxy and sizing variables, uncomment and fully wire the ssm/rds_tenant/rds_proxy/rds_control_plane module calls using underscore labels, and uncomment/add the three RDS + SSM provisioner contract outputs. Then run make plan-check to confirm the phase gate is green.

Purpose: This plan closes the loop — the modules are implemented in Plans 01-04 but produce nothing in the plan until they are wired. envs/prod/main.tf is the single integration point (no cross-module calls). The provisioner contract outputs (tenant_rds_endpoint, rds_proxy_endpoint, control_plane_rds_endpoint) complete the AwsDeploymentAdapter interface for Phase 3.

Output: envs/prod/{versions,variables,main,outputs}.tf updated. make plan-check green with both RDS instances and 3 SSM parameters in the plan, 0 proxy resources with default flag.
</objective>

<execution_context>
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-CONTEXT.md
@/home/yevhenii/.claude/get-shit-done/workflows/execute-plan.md
@/home/yevhenii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/yevhenii/projects/saas/platform-terraform/.planning/ROADMAP.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-RESEARCH.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-PATTERNS.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-01-SUMMARY.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-02-SUMMARY.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-03-SUMMARY.md
@/home/yevhenii/projects/saas/platform-terraform/.planning/phases/03-databases-and-secrets/03-04-SUMMARY.md

<interfaces>
<!-- Self-reference: envs/prod files — the stubs are already present; uncomment and expand -->

From envs/prod/versions.tf (current — add random block inside required_providers):
  terraform {
    required_version = ">= 1.11.0"
    required_providers {
      aws = { source = "hashicorp/aws", version = "~> 6.0" }
      # ADD: random = { source = "hashicorp/random", version = "~> 3.0" }
    }
  }

From envs/prod/main.tf (stubs to uncomment and expand, lines 35-49):
  # module "rds_tenant" { source = "../../modules/rds-tenant", name_prefix = local.name_prefix, subnet_ids = module.networking.private_subnet_ids }
  # module "rds_proxy" { source = "../../modules/rds-proxy", name_prefix = local.name_prefix }
  # module "rds_control_plane" { source = "../../modules/rds-control-plane", name_prefix = local.name_prefix }
  # module "ssm" { source = "../../modules/ssm", name_prefix = local.name_prefix }

From envs/prod/outputs.tf (stubs to uncomment, lines 36-44):
  # output "tenant_rds_endpoint" { ... value = module.rds_tenant.endpoint }
  # output "rds_proxy_endpoint" { ... value = module.rds_proxy.endpoint }
  (control_plane_rds_endpoint has no stub — must be added fresh)

From envs/prod/variables.tf (existing pattern — add new variables using same block style):
  variable "region" { description "...", type = string, default = "us-east-1" }

Existing module call pattern (lines 15-20, 23-26, 29-32 — use exact same style):
  module "networking" {
    source      = "../../modules/networking"
    name_prefix = local.name_prefix
    vpc_cidr    = var.vpc_cidr
    azs         = var.azs
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update envs/prod/versions.tf, envs/prod/variables.tf, envs/prod/main.tf, and envs/prod/outputs.tf</name>
  <files>envs/prod/versions.tf, envs/prod/variables.tf, envs/prod/main.tf, envs/prod/outputs.tf</files>

  <read_first>
    - envs/prod/versions.tf (current — add random provider)
    - envs/prod/variables.tf (current — add enable_rds_proxy + sizing vars using existing block pattern)
    - envs/prod/main.tf (current — uncomment and expand 4 module stubs; CRITICAL: use underscore labels)
    - envs/prod/outputs.tf (current — uncomment 2 stubs, add 1 new output)
    - .planning/phases/03-databases-and-secrets/03-PATTERNS.md §envs/prod/versions.tf + §envs/prod/variables.tf + §envs/prod/main.tf + §envs/prod/outputs.tf
    - .planning/phases/03-databases-and-secrets/03-RESEARCH.md §Offline Plan Landmines (L-4, L-5, L-6) + §Pitfall 6: Module Label Hyphen vs Underscore
    - modules/ssm/outputs.tf (Plan 01 — exports: tenant_rds_password [sensitive], cp_rds_password [sensitive], tenant_rds_password_name, tenant_rds_password_arn, cp_rds_password_name, cp_rds_password_arn, hmac_salt_name, hmac_salt_arn)
    - modules/rds-tenant/outputs.tf (Plan 02 — exports: endpoint, identifier, security_group_id, db_resource_id)
    - modules/rds-proxy/outputs.tf (Plan 04 — exports: endpoint [try() guarded])
    - modules/rds-control-plane/outputs.tf (Plan 03 — exports: endpoint, security_group_id)
  </read_first>

  <action>
  Edit envs/prod/versions.tf: add the random provider block inside the existing required_providers block, after the aws block:
    random = { source = "hashicorp/random", version = "~> 3.0" }
  This fixes L-5 (hashicorp/random not in required_providers causes terraform init to fail — random_password in modules/ssm requires it). Leave all other content unchanged.

  Edit envs/prod/variables.tf: append new variable blocks at the end of the file using the same block format as existing variables (description ending with period, type constraint, default):
    - enable_rds_proxy: description "Enable the RDS Proxy module. Set true at ~30 active tenants.", type = bool, default = false
    - rds_instance_class: description "RDS instance class for both tenant and control-plane instances.", type = string, default = "db.t4g.small"
    - rds_engine_version: description "PostgreSQL engine version for all RDS instances.", type = string, default = "16"
    - rds_allocated_storage: description "Initial allocated storage in GiB for each RDS instance.", type = number, default = 20
    - rds_max_allocated_storage: description "Maximum allocated storage for autoscaling in GiB.", type = number, default = 100

  Edit envs/prod/main.tf — replace the commented section at lines 34-49 (databases) and the commented ssm lines (lines 73-77). Do NOT touch any existing uncommented module calls (networking, ecr, ecs lines 14-32). Replace the stubs with fully-wired calls:

  Section comment: # --- 5. Databases and secrets -------------------------------------------------

  module "ssm" — MUST come first (rds modules need its sensitive output):
    source      = "../../modules/ssm"
    name_prefix = local.name_prefix

  module "rds_tenant" (underscore label — CRITICAL per CONCERNS.md fragile area and RESEARCH.md Pitfall 6; the existing commented outputs.tf reference uses module.rds_tenant.*):
    source                 = "../../modules/rds-tenant"
    name_prefix            = local.name_prefix
    subnet_ids             = module.networking.private_subnet_ids
    vpc_id                 = module.networking.vpc_id
    task_security_group_id = module.networking.task_security_group_id
    master_password        = module.ssm.tenant_rds_password

  module "rds_proxy" (underscore label):
    source                 = "../../modules/rds-proxy"
    name_prefix            = local.name_prefix
    enable_rds_proxy       = var.enable_rds_proxy
    db_instance_identifier = module.rds_tenant.identifier
    subnet_ids             = module.networking.private_subnet_ids
    vpc_id                 = module.networking.vpc_id
    task_security_group_id = module.networking.task_security_group_id
    master_username        = "odoo_master"
    master_password        = module.ssm.tenant_rds_password

  module "rds_control_plane" (underscore label):
    source                 = "../../modules/rds-control-plane"
    name_prefix            = local.name_prefix
    subnet_ids             = module.networking.private_subnet_ids
    vpc_id                 = module.networking.vpc_id
    task_security_group_id = module.networking.task_security_group_id
    master_password        = module.ssm.cp_rds_password

  Remove the commented # module "ssm" block from the filestore/routing section (lines 73-77) — ssm is now wired above.

  Edit envs/prod/outputs.tf — uncomment the two existing stubs and add one new output:

  Uncomment output "tenant_rds_endpoint":
    description = "Shared tenant RDS endpoint -> provisioner `aws_shared_rds_endpoint`."
    value       = module.rds_tenant.endpoint

  Uncomment output "rds_proxy_endpoint":
    description = "RDS Proxy endpoint -> provisioner `aws_rds_proxy_endpoint`."
    value       = module.rds_proxy.endpoint

  Add new output "control_plane_rds_endpoint" (no existing stub — use the same description convention):
    description = "Control-plane RDS endpoint -> provisioner `aws_control_plane_rds_endpoint`."
    value       = module.rds_control_plane.endpoint

  CRITICAL module label rule: module "rds_tenant", module "rds_proxy", module "rds_control_plane" (underscore). Never module "rds-tenant" (hyphen) — the existing commented outputs already reference module.rds_tenant.endpoint so hyphen labels would silently break them (RESEARCH.md Pitfall 6).

  CRITICAL SSM output rule: SSM exports module.ssm.tenant_rds_password as a sensitive pass-through to rds modules ONLY. Do NOT add module.ssm.tenant_rds_password as an envs/prod output (that would expose the raw password in the provisioner contract). Only add SSM name/ARN outputs if specifically needed by the provisioner contract — they are available as module.ssm.tenant_rds_password_name, module.ssm.hmac_salt_name etc. but are not part of the ROADMAP Phase 3 contract outputs listed, so do not add them unless the ROADMAP outputs.tf contract table requires them.

  Section structure in main.tf: the combined databases+secrets section replaces both the "# --- 5. Databases" block and the "# --- 6. Filestore, routing/TLS, secrets" ssm portion. The efs/acm/alb/route53 stubs in section 6 must remain commented (they belong to Phases 4-5).
  </action>

  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep -c "hashicorp/random" envs/prod/versions.tf && grep -c "enable_rds_proxy" envs/prod/variables.tf && grep -c "module \"rds_tenant\"" envs/prod/main.tf && grep -c "module.ssm.tenant_rds_password" envs/prod/main.tf && grep -c "tenant_rds_endpoint" envs/prod/outputs.tf</automated>
  </verify>

  <acceptance_criteria>
    - envs/prod/versions.tf contains random = { source = "hashicorp/random", version = "~> 3.0" } inside required_providers
    - envs/prod/variables.tf contains variable "enable_rds_proxy" with type = bool and default = false
    - envs/prod/main.tf contains uncommented module "ssm" block (no leading # on the module keyword line)
    - envs/prod/main.tf contains module "rds_tenant" (underscore — NOT "rds-tenant")
    - envs/prod/main.tf contains module "rds_proxy" (underscore)
    - envs/prod/main.tf contains module "rds_control_plane" (underscore)
    - envs/prod/main.tf module "rds_tenant" includes master_password = module.ssm.tenant_rds_password
    - envs/prod/main.tf module "rds_control_plane" includes master_password = module.ssm.cp_rds_password
    - envs/prod/main.tf module "rds_proxy" includes db_instance_identifier = module.rds_tenant.identifier
    - envs/prod/main.tf module "rds_proxy" includes enable_rds_proxy = var.enable_rds_proxy
    - envs/prod/outputs.tf contains active (uncommented) output "tenant_rds_endpoint"
    - envs/prod/outputs.tf contains active (uncommented) output "rds_proxy_endpoint"
    - envs/prod/outputs.tf contains active output "control_plane_rds_endpoint"
    - envs/prod/outputs.tf does NOT contain any output referencing module.ssm.tenant_rds_password or module.ssm.cp_rds_password (sensitive raw values must not cross the provisioner contract boundary)
    - The efs, acm, alb, route53 module stubs in envs/prod/main.tf remain commented out
  </acceptance_criteria>

  <done>envs/prod/{versions,variables,main,outputs}.tf updated: random provider added, enable_rds_proxy and 4 sizing vars declared, 4 new module calls wired with underscore labels and correct sensitive password pass-through, 3 RDS endpoint outputs active.</done>
</task>

<task type="auto">
  <name>Task 2: Run make plan-check — offline fmt/validate/plan gate</name>
  <files></files>

  <read_first>
    - Makefile (to confirm make plan-check target and its commands)
    - envs/prod/main.tf (just edited — confirm 4 new module calls are present and uncommented)
    - envs/prod/outputs.tf (just edited — confirm 3 new outputs are active)
    - .planning/phases/03-databases-and-secrets/03-RESEARCH.md §Offline Plan Landmines (diagnose any failure against L-1 through L-8)
    - ~/.claude/projects/-home-yevhenii-projects-saas-platform-terraform/memory/terraform-offline-plan-gate.md (MEMORY.md reference — use make plan-check not make plan; terraform at ~/.local/bin)
  </read_first>

  <action>
  Run the offline gate: make plan-check from the repo root. This runs terraform fmt -check, terraform validate, and terraform plan with dummy AWS credentials in envs/prod.

  If the plan succeeds:
    - Capture the resource count from the plan output. Phase 2 completed with 13 resources; Phase 3 should add at minimum: 3 random_password, 3 aws_ssm_parameter, 1 aws_security_group (rds_tenant), 1 aws_db_subnet_group (tenant), 1 aws_db_parameter_group (tenant), 1 aws_db_instance (tenant), 1 aws_security_group (cp_rds), 1 aws_db_subnet_group (cp), 1 aws_db_parameter_group (cp), 1 aws_db_instance (control_plane) = 15 new resources, total ~28+.
    - Confirm 0 aws_db_proxy resources appear in the plan (enable_rds_proxy defaults to false).
    - Confirm aws_ssm_parameter resources appear with type = "SecureString" (visible in plan output).

  If fmt-check fails: run make fmt (or terraform fmt -recursive from repo root), then re-run make plan-check.

  If validate fails with "Unsupported argument": a variable was not pre-declared in a module's variables.tf. Diagnose which module and argument from the error message; add the missing variable to that module's variables.tf, then re-run.

  If validate fails with "random_password not found": hashicorp/random is not in required_providers yet. Verify envs/prod/versions.tf was edited correctly.

  If plan fails with index-out-of-range on proxy endpoint: the try() guard is missing from modules/rds-proxy/outputs.tf. Add try(aws_db_proxy.this[0].endpoint, null) and re-run.

  Do NOT diagnose errors by guessing — read the exact error message from the make plan-check output, map it to the matching landmine in RESEARCH.md §Offline Plan Landmines, and apply the documented fix.
  </action>

  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && make plan-check 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - make plan-check exits 0 (no non-zero exit code)
    - terraform fmt -check step produces no output (all files formatted)
    - terraform validate step outputs "Success! The configuration is valid."
    - terraform plan output contains at least one aws_db_instance resource (non-empty plan)
    - terraform plan output contains at least one aws_ssm_parameter resource
    - terraform plan output shows 0 resources of type aws_db_proxy (proxy flag defaults to false)
    - Plan total resource count is greater than 13 (Phase 2 baseline — new resources added this phase)
  </acceptance_criteria>

  <done>make plan-check exits 0; plan is non-empty with both RDS instances (tenant Single-AZ + control-plane Multi-AZ) and 3 SSM SecureString parameters visible; 0 proxy resources; terraform fmt-check and validate pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module.ssm.tenant_rds_password → module.rds_tenant | Sensitive password passes between modules in-memory at plan/apply time; never written to a root output |
| module.ssm.cp_rds_password → module.rds_control_plane | Same as above for control-plane password |
| envs/prod/outputs.tf → provisioner adapter | Only non-sensitive endpoint strings (RDS addresses) cross this boundary; sensitive passwords are blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Information Disclosure | envs/prod/outputs.tf — sensitive password leak risk | mitigate | Acceptance criteria explicitly verifies that module.ssm.tenant_rds_password and module.ssm.cp_rds_password do NOT appear in envs/prod/outputs.tf; only endpoint strings (not passwords) cross the provisioner contract boundary |
| T-03-17 | Tampering | module label hyphen vs underscore | mitigate | Acceptance criteria verifies module "rds_tenant" (underscore), not "rds-tenant"; RESEARCH.md Pitfall 6 is the documented risk; verify gate catches this via terraform validate (undefined output reference if wrong) |
| T-03-18 | Spoofing | hashicorp/random missing from required_providers | mitigate | Acceptance criteria verifies random block in versions.tf; make plan-check catches this at terraform init step (L-5 landmine) |
</threat_model>

<verification>
Phase 3 complete when:
- envs/prod/versions.tf: hashicorp/random ~> 3.0 in required_providers
- envs/prod/variables.tf: enable_rds_proxy (bool, false) + 4 rds sizing vars
- envs/prod/main.tf: module "ssm", "rds_tenant", "rds_proxy", "rds_control_plane" all uncommented, underscore labels, correct sensitive password wiring
- envs/prod/outputs.tf: tenant_rds_endpoint, rds_proxy_endpoint, control_plane_rds_endpoint all active; no sensitive password values
- make plan-check exits 0 with both RDS instances, 3 SSM params, 0 proxy resources in plan
</verification>

<success_criteria>
All Phase 3 modules are wired into envs/prod and the offline gate is green: make plan-check passes (fmt-check + validate + non-empty plan) with SSM SecureString parameters, tenant Single-AZ RDS, control-plane Multi-AZ RDS visible in the plan and zero proxy resources with the default flag. The three RDS endpoint outputs are live in envs/prod/outputs.tf completing the AwsDeploymentAdapter provisioner contract for Phase 3.
</success_criteria>

<output>
Create .planning/phases/03-databases-and-secrets/03-05-SUMMARY.md when done.
</output>
