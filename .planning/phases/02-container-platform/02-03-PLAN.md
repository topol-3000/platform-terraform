---
phase: 02-container-platform
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - envs/prod/main.tf
  - envs/prod/outputs.tf
autonomous: true
requirements: [ECR-02, ECS-02, VER-01]
must_haves:
  truths:
    - "The module \"ecr\" call in envs/prod/main.tf is uncommented and resolves to ../../modules/ecr, and the ecr_image_uri / ecs_cluster_arn outputs are uncommented (D-06)"
    - "The module \"ecs\" call in envs/prod/main.tf is uncommented and resolves to ../../modules/ecs"
    - "The ecr_image_uri output is uncommented and = module.ecr.image_uri with managed-repository wording (no pull-through)"
    - "The ecs_cluster_arn output is uncommented and = module.ecs.cluster_arn"
    - "make plan-check passes (offline gate: fmt -check, validate, and a non-empty plan including the ECR repository and ECS cluster); no skip_* provider flags, no live-call data sources (D-07)"
  artifacts:
    - path: "envs/prod/main.tf"
      provides: "Live module ecr (build step 3) and module ecs (build step 4) calls"
      contains: "module \"ecr\""
    - path: "envs/prod/outputs.tf"
      provides: "Live ecr_image_uri and ecs_cluster_arn contract outputs"
      contains: "module.ecr.image_uri"
  key_links:
    - from: "envs/prod/main.tf module \"ecr\""
      to: "modules/ecr"
      via: "source = ../../modules/ecr, name_prefix = local.name_prefix"
      pattern: "module \"ecr\""
    - from: "envs/prod/outputs.tf ecr_image_uri"
      to: "module.ecr.image_uri"
      via: "value = module.ecr.image_uri"
      pattern: "module\\.ecr\\.image_uri"
    - from: "envs/prod/outputs.tf ecs_cluster_arn"
      to: "module.ecs.cluster_arn"
      via: "value = module.ecs.cluster_arn"
      pattern: "module\\.ecs\\.cluster_arn"
---

<objective>
Wire `modules/ecr` and `modules/ecs` into `envs/prod` (per CONTEXT.md D-06): uncomment the `module "ecr"` (build step 3) and `module "ecs"` (build step 4) calls in `envs/prod/main.tf`, uncomment the `ecr_image_uri` and `ecs_cluster_arn` contract outputs in `envs/prod/outputs.tf` (rewriting the stale "pull-through image URI" description to the managed-repository wording), and confirm the offline `make plan-check` gate stays green with the new resources in the plan (VER-01).

Purpose: `envs/prod/outputs.tf` is the typed contract to the provisioner `AwsDeploymentAdapter`. This plan closes the ECR-02 / ECS-02 wiring half and proves Phase 2 is code-complete via the offline gate.

Output: Edited `envs/prod/main.tf` and `envs/prod/outputs.tf`; a green `make plan-check` whose plan output includes `aws_ecr_repository` and `aws_ecs_cluster`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-container-platform/02-CONTEXT.md
@.planning/phases/02-container-platform/02-PATTERNS.md
@.planning/phases/02-container-platform/02-01-SUMMARY.md
@.planning/phases/02-container-platform/02-02-SUMMARY.md

# Decision anchors (CONTEXT.md):
# - D-06: uncomment module "ecr" (step 3) + module "ecs" (step 4) in envs/prod/main.tf,
#   and ecr_image_uri (= module.ecr.image_uri) / ecs_cluster_arn (= module.ecs.cluster_arn) in envs/prod/outputs.tf.
#   The ecr_image_uri stub description STILL says "pull-through image URI" — REWRITE it to the managed-repository wording.
# - D-07: verification is the offline `make plan-check` gate. No skip_* flags in providers.tf. No live-call data sources.
# - AUTO-MEMORY: use `make plan-check` (offline gate), NEVER `make plan` (which needs real AWS/S3).

# Both ECR/ECS module calls take ONLY name_prefix = local.name_prefix (ECR/ECS consume no networking outputs).
# Module labels are single words (ecr, ecs) — underscore rule is moot here but keep snake_case.

<interfaces>
<!-- These module outputs already exist after plans 02-01 / 02-02: -->
<!-- module.ecr.image_uri    = aws_ecr_repository.<label>.repository_url -->
<!-- module.ecs.cluster_arn  = aws_ecs_cluster.<label>.arn -->
<!-- local.name_prefix = "${var.project}-${var.environment}" is defined in envs/prod/main.tf lines 10-12 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uncomment and wire the ecr/ecs module calls and their contract outputs</name>
  <files>envs/prod/main.tf, envs/prod/outputs.tf</files>
  <read_first>
    - envs/prod/main.tf (the commented module "ecr" block lines 22-26 and module "ecs" block lines 28-32; the live module "networking" block lines 14-20 is the canonical wiring style; local.name_prefix lines 10-12)
    - envs/prod/outputs.tf (the commented ecs_cluster_arn block lines 6-9 and ecr_image_uri block lines 61-64; the live private_subnet_ids block lines 11-14 is the canonical output style; note ecr_image_uri description still says "pull-through image URI" — must be rewritten)
    - modules/ecr/outputs.tf (confirm the exact output name is image_uri — produced by plan 02-01)
    - modules/ecs/outputs.tf (confirm the exact output name is cluster_arn — produced by plan 02-02)
  </read_first>
  <action>
    Edit (do NOT rewrite) the two root files.

    1. In envs/prod/main.tf: uncomment the step-3 `module "ecr"` block (source = "../../modules/ecr", name_prefix = local.name_prefix) and the step-4 `module "ecs"` block (source = "../../modules/ecs", name_prefix = local.name_prefix). Keep the existing "# --- 3. ..." / "# --- 4. ..." section banner comments but update the step-3 banner text away from "ECR pull-through cache (odoo-core image from GHCR)" to the managed-repository wording per D-01 (e.g. "Managed ECR repository (odoo-core image)"). Each block's sole input is name_prefix = local.name_prefix; neither references module.networking.* (ECR/ECS consume no networking outputs).

    2. In envs/prod/outputs.tf: uncomment the `ecs_cluster_arn` output (value = module.ecs.cluster_arn) — its description is already correct. Uncomment the `ecr_image_uri` output (value = module.ecr.image_uri) and REWRITE its description from the stale "ECR pull-through image URI -> provisioner `aws_ecr_image`." to managed-repository wording following the CONVENTIONS contract format "<What it is> -> provisioner `<setting_name>`.", e.g. "ECR repository URL for odoo-core -> provisioner `aws_ecr_image`."

    3. Do NOT touch envs/prod/providers.tf or envs/prod/backend.tf (D-07 — provider stays clean; the offline gate's skip_* flags live only in the Makefile's transient gate_override.tf).
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep -Eq '^[[:space:]]*module "ecr"' envs/prod/main.tf && grep -Eq '^[[:space:]]*module "ecs"' envs/prod/main.tf && grep -Eq '^[[:space:]]*output "ecr_image_uri"' envs/prod/outputs.tf && grep -q 'module.ecr.image_uri' envs/prod/outputs.tf && grep -Eq '^[[:space:]]*output "ecs_cluster_arn"' envs/prod/outputs.tf && grep -q 'module.ecs.cluster_arn' envs/prod/outputs.tf && ! grep -iq 'pull-through' envs/prod/outputs.tf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq '^[[:space:]]*module "ecr"' envs/prod/main.tf` and `grep -Eq '^[[:space:]]*module "ecs"' envs/prod/main.tf` both succeed (calls uncommented, not inside a comment).
    - Both module calls set `name_prefix = local.name_prefix` and neither references `module.networking.*`.
    - `grep -Eq '^[[:space:]]*output "ecr_image_uri"' envs/prod/outputs.tf` and the output's value is `module.ecr.image_uri`.
    - `grep -Eq '^[[:space:]]*output "ecs_cluster_arn"' envs/prod/outputs.tf` and the output's value is `module.ecs.cluster_arn`.
    - `grep -iq 'pull-through' envs/prod/outputs.tf` returns NOTHING (stale description rewritten); the step-3 banner in main.tf no longer says "pull-through" / "GHCR".
    - `envs/prod/providers.tf` and `envs/prod/backend.tf` are unchanged (no `skip_*` flags added).
  </acceptance_criteria>
  <done>
    Both module calls and both contract outputs are uncommented and resolve to the correct module attributes; all "pull-through"/GHCR wording is gone from the root files; providers.tf/backend.tf are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the offline plan-check gate and confirm ECR + ECS appear in the plan</name>
  <files>envs/prod/main.tf, envs/prod/outputs.tf</files>
  <read_first>
    - Makefile (the plan-check target lines 31-48 — the offline gate; writes a transient gate_override.tf with skip_* flags + local backend, runs fmt -check / validate / plan, always cleans up)
    - $HOME/.claude/projects/-home-yevhenii-projects-saas-platform-terraform/memory/terraform-offline-plan-gate.md (terraform is in ~/.local/bin; use `make plan-check`, never `make plan`)
  </read_first>
  <action>
    From the repo root, first run `terraform fmt -recursive` (formats the new module files plus root edits so the gate's fmt -check passes), then run the offline gate `make plan-check`. terraform lives in ~/.local/bin — ensure it is on PATH (export PATH="$HOME/.local/bin:$PATH"). NEVER run `make plan` or `terraform apply` (those need real AWS/S3 and would fail on credentials or incur spend). The gate must report fmt -check clean, validate success, and a non-empty plan. Confirm both `aws_ecr_repository` and `aws_ecs_cluster` appear in the plan output and the total resource count exceeds Phase 1's 9 resources. If anything fails, fix the offending module/root file (this is the integration gate for plans 02-01 / 02-02 / this plan) and re-run until green.
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && export PATH="$HOME/.local/bin:$PATH" && terraform fmt -recursive >/dev/null && out=$(make plan-check 2>&1); echo "$out"; echo "$out" | grep -q 'aws_ecr_repository' && echo "$out" | grep -q 'aws_ecs_cluster' && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `make plan-check` exits 0: `terraform fmt -check -recursive` passes, `terraform validate` passes, `terraform plan` produces a non-empty plan.
    - The plan output includes a resource named `aws_ecr_repository` (the odoo-core repo) and `aws_ecs_cluster` (the shared cluster).
    - The plan's total resource count is greater than Phase 1's 9 (new ECR repo + lifecycle policy + ECS cluster + capacity-providers association).
    - No `gate_override.tf` is left behind in `envs/prod/` after the run (the Makefile trap removes it).
    - `make plan` and `terraform apply` are NOT run.
  </acceptance_criteria>
  <done>
    `make plan-check` is green with `aws_ecr_repository` and `aws_ecs_cluster` in a non-empty plan whose resource count exceeds 9; no transient gate_override.tf remains; Phase 2 is code-complete (VER-01 satisfied).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terraform state / outputs → provisioner adapter | `ecr_image_uri` and `ecs_cluster_arn` are read as runtime settings |
| Offline plan gate → real provider config | The transient gate_override.tf must not leak skip_* flags into providers.tf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Stale gate_override.tf merging skip_* flags into real provider | mitigate | Makefile trap (EXIT INT TERM) removes gate_override.tf; real plan/apply also delete it first; this plan asserts none remains after the run |
| T-02-09 | Information Disclosure | Secrets in contract outputs | accept | Both outputs are non-secret identifiers (repo URL, cluster ARN); no `sensitive` data exposed — consistent with the SSM-only secrets rule |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — Terraform providers resolve from the registry; not applicable |
</threat_model>

<verification>
- `grep -Eq '^[[:space:]]*module "ecr"' envs/prod/main.tf` and `^[[:space:]]*module "ecs"` (calls live, not commented)
- `grep -q 'module.ecr.image_uri' envs/prod/outputs.tf` and `grep -q 'module.ecs.cluster_arn' envs/prod/outputs.tf`
- `! grep -iq 'pull-through' envs/prod/outputs.tf` (stale wording gone)
- `make plan-check` exits 0 with `aws_ecr_repository` + `aws_ecs_cluster` in the plan and resource count > 9
- No `gate_override.tf` left in `envs/prod/`
</verification>

<success_criteria>
- The `module "ecr"` and `module "ecs"` calls in `envs/prod/main.tf` are uncommented and resolve (ECR-02 / ECS-02 wiring).
- The `ecr_image_uri` (= module.ecr.image_uri) and `ecs_cluster_arn` (= module.ecs.cluster_arn) outputs are uncommented, resolve, and carry managed-repository (non-pull-through) descriptions.
- `make plan-check` passes with the ECR and ECS resources in a non-empty plan (VER-01).
- providers.tf / backend.tf untouched; no transient override left behind.
</success_criteria>

<output>
Create `.planning/phases/02-container-platform/02-03-SUMMARY.md` when done.
</output>
