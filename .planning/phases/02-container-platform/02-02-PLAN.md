---
phase: 02-container-platform
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/ecs/main.tf
  - modules/ecs/outputs.tf
autonomous: true
requirements: [ECS-01, ECS-02]
must_haves:
  truths:
    - "A shared ECS/Fargate cluster named with var.name_prefix exists in the module"
    - "An aws_ecs_cluster_capacity_providers association wires FARGATE and FARGATE_SPOT to the cluster"
    - "Container Insights is enabled on the cluster"
    - "The module exports cluster_arn from the cluster resource's arn attribute"
    - "No data source is introduced (keeps the offline plan gate intact)"
  artifacts:
    - path: "modules/ecs/main.tf"
      provides: "Shared ECS cluster + FARGATE/FARGATE_SPOT capacity providers + Container Insights"
      contains: "resource \"aws_ecs_cluster\""
    - path: "modules/ecs/outputs.tf"
      provides: "cluster_arn output = cluster arn attribute"
      exports: ["cluster_arn"]
  key_links:
    - from: "modules/ecs/main.tf"
      to: "modules/ecs/main.tf"
      via: "aws_ecs_cluster_capacity_providers.cluster_name = aws_ecs_cluster.<label>.name"
      pattern: "aws_ecs_cluster_capacity_providers"
    - from: "modules/ecs/outputs.tf"
      to: "modules/ecs/main.tf"
      via: "cluster_arn value = aws_ecs_cluster.<label>.arn"
      pattern: "\\.arn"
---

<objective>
Implement `modules/ecs` as a shared ECS/Fargate cluster for the tenant fleet (per CONTEXT.md D-04/D-05): an `aws_ecs_cluster` with Container Insights enabled, PLUS an `aws_ecs_cluster_capacity_providers` association wiring `FARGATE` + `FARGATE_SPOT`. Export `cluster_arn`.

Purpose: The provisioner adapter creates only per-tenant services/tasks at runtime, so the cluster and its capacity-provider wiring belong in the shared baseline. FARGATE_SPOT enables cost-optimized scheduling; Container Insights gives fleet-wide observability.

Output: Implemented `modules/ecs/main.tf` and `modules/ecs/outputs.tf`. Wiring into `envs/prod` is handled by plan 02-03 (Wave 2), so this plan does NOT touch `envs/prod`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-container-platform/02-CONTEXT.md
@.planning/phases/02-container-platform/02-PATTERNS.md

# Decision anchors (CONTEXT.md):
# - D-04: aws_ecs_cluster PLUS aws_ecs_cluster_capacity_providers wiring FARGATE + FARGATE_SPOT, and Container Insights enabled.
# - D-05: export cluster_arn; cluster is name_prefix-named.
# - D-07: no data sources, no skip_* provider flags — keep the plan fully offline.
# - Claude's Discretion: containerInsights value (enabled vs enhanced); whether to set default_capacity_provider_strategy; single-resource vs count/for_each style.

# IMPORTANT — the existing stub header ("Fargate launch type at MVP") is close but the
# STATUS-stub and TODO lines must be rewritten; keep a SEED-001 note.

<interfaces>
<!-- Module interface contract the root wiring (plan 02-03) depends on. -->
<!-- modules/ecs exposes exactly one output: -->
output "cluster_arn"  # value = aws_ecs_cluster.<label>.arn
<!-- Sole input variable (already present in modules/ecs/variables.tf, do not change): -->
variable "name_prefix" { type = string }
<!-- Root expects: module.ecs.cluster_arn (see envs/prod/main.tf line 28-32, envs/prod/outputs.tf line 6-9) -->
<!-- ECS consumes NO networking outputs — name_prefix is the only input. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the shared ECS cluster, capacity-provider association, and outputs</name>
  <files>modules/ecs/main.tf, modules/ecs/outputs.tf</files>
  <read_first>
    - modules/ecs/main.tf (current stub — header intent is close but STATUS/TODO lines must be rewritten)
    - modules/ecs/outputs.tf (current stub — comment-only, to be replaced with a real output)
    - modules/ecs/variables.tf (the name_prefix input — already correct, do NOT modify)
    - modules/networking/main.tf (canonical module file structure: header comment, WHY-comment-above-each-resource, Name-tag-only pattern, "${var.name_prefix}-<thing>" naming, snake_case labels)
    - modules/networking/outputs.tf (canonical output pattern: description ends with a period, value = resource attribute)
    - bootstrap/main.tf (multi-resource-same-label pattern — the capacity-providers association shares the cluster's logical label)
  </read_first>
  <action>
    Rewrite modules/ecs/main.tf and modules/ecs/outputs.tf to implement the shared ECS/Fargate cluster (D-04/D-05).

    1. Rewrite the file-header comment following the networking style (# Module: ecs / # Purpose: ... / SEED-001 note). Purpose: shared ECS/Fargate cluster for the tenant fleet; the provisioner creates per-tenant services/tasks at runtime. Keep a SEED-001 note. REMOVE the STATUS-stub and TODO lines.

    2. Declare `resource "aws_ecs_cluster"` with a logical label (e.g. `main`, snake_case). Set `name = "${var.name_prefix}-cluster"` (name_prefix-named per D-05). Enable Container Insights via a `setting` block: `setting { name = "containerInsights" value = "enabled" }` (Claude's Discretion: "enabled" vs "enhanced" — choose "enabled" for predictable cost, add a one-line WHY comment). Add only a `Name` tag: `tags = { Name = "${var.name_prefix}-cluster" }`; do NOT re-declare default_tags keys.

    3. Declare `resource "aws_ecs_cluster_capacity_providers"` sharing the cluster's logical label (multi-resource-same-label pattern). Set `cluster_name = aws_ecs_cluster.<label>.name` and `capacity_providers = ["FARGATE", "FARGATE_SPOT"]`. Optionally set a `default_capacity_provider_strategy` block (Claude's Discretion) — if included, weight FARGATE as the safe default with FARGATE_SPOT as an additional provider, each with a one-line WHY comment. Add a WHY comment above the resource (cost-optimized scheduling + Fargate serverless baseline).

    4. Replace modules/ecs/outputs.tf comment-only stub with a single `output "cluster_arn"` whose value is `aws_ecs_cluster.<label>.arn` (D-05). Description: one sentence ending with a period, e.g. "Shared ECS cluster ARN for the tenant Fargate fleet." No `sensitive` annotation.

    5. Introduce NO `data` sources and NO new variables (D-07) — ECS consumes no networking outputs; name_prefix is the only input.
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep -q 'resource "aws_ecs_cluster"' modules/ecs/main.tf && grep -q 'name_prefix}-cluster' modules/ecs/main.tf && grep -q 'containerInsights' modules/ecs/main.tf && grep -q 'resource "aws_ecs_cluster_capacity_providers"' modules/ecs/main.tf && grep -q 'FARGATE_SPOT' modules/ecs/main.tf && grep -q '\.arn' modules/ecs/outputs.tf && ! grep -Eq '^\s*data "' modules/ecs/main.tf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `modules/ecs/main.tf` contains `resource "aws_ecs_cluster"` with `name = "${var.name_prefix}-cluster"` and a `Name`-only tag.
    - `modules/ecs/main.tf` contains a `setting` block with `name = "containerInsights"` and a non-empty `value`.
    - `modules/ecs/main.tf` contains `resource "aws_ecs_cluster_capacity_providers"` with `capacity_providers = ["FARGATE", "FARGATE_SPOT"]` and `cluster_name` referencing the cluster by `.name`.
    - `modules/ecs/outputs.tf` defines `output "cluster_arn"` with `value = aws_ecs_cluster.<label>.arn`.
    - No `data` block and no new `variable` block exist anywhere under `modules/ecs/`.
    - The stub's STATUS / TODO lines are removed from the header.
    - `terraform fmt -check modules/ecs` exits 0 (run `terraform fmt modules/ecs` first if needed).
  </acceptance_criteria>
  <done>
    The ecs module declares a name_prefix-named ECS cluster with Container Insights, a FARGATE + FARGATE_SPOT capacity-provider association, and exports `cluster_arn` from the cluster's `arn` attribute; no data sources or extra variables exist. `terraform fmt -check` is clean for the module. Note: a full `make plan-check` runs in plan 02-03 after wiring — do NOT wire envs/prod here.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant tasks → shared ECS cluster | All tenant Fargate tasks schedule onto one shared control plane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Repudiation | Lack of cluster-level observability | mitigate | `setting { name = "containerInsights" value = "enabled" }` provides metrics/logging for audit and incident response |
| T-02-06 | Denial of Service | Capacity exhaustion / runaway Fargate cost | accept | FARGATE + FARGATE_SPOT mix bounds cost; per-tenant task limits are enforced by the provisioner adapter at runtime, not by the baseline |
| T-02-07 | Elevation of Privilege | Task IAM roles | accept | Task execution/role IAM is attached per-task by the provisioner adapter at runtime; out of scope for the shared cluster baseline |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — not applicable |
</threat_model>

<verification>
- `grep -q 'resource "aws_ecs_cluster"' modules/ecs/main.tf` (cluster present)
- `grep -q 'resource "aws_ecs_cluster_capacity_providers"' modules/ecs/main.tf` and `grep -q 'FARGATE_SPOT' modules/ecs/main.tf` (capacity providers wired)
- `grep -q 'containerInsights' modules/ecs/main.tf` (observability enabled)
- `grep -q '\.arn' modules/ecs/outputs.tf` (cluster_arn from attribute)
- `terraform fmt -check modules/ecs` exits 0
- Full offline gate (`make plan-check`) is verified in plan 02-03 after wiring.
</verification>

<success_criteria>
- modules/ecs declares a shared ECS/Fargate cluster (ECS-01) named with `var.name_prefix`, with Container Insights and a FARGATE + FARGATE_SPOT capacity-provider association (D-04).
- modules/ecs exports `cluster_arn = aws_ecs_cluster.<label>.arn` (ECS-02 module side).
- No data sources or extra variables introduced.
- Module formats clean (`terraform fmt -check`).
</success_criteria>

<output>
Create `.planning/phases/02-container-platform/02-02-SUMMARY.md` when done.
</output>
