---
phase: 02-container-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/ecr/main.tf
  - modules/ecr/outputs.tf
autonomous: true
requirements: [ECR-01, ECR-02]
must_haves:
  truths:
    - "A managed aws_ecr_repository for the odoo-core image exists in the module (NOT a pull-through cache)"
    - "The repository name carries var.name_prefix"
    - "The module exports image_uri derived from the repository's repository_url attribute (no hand-built account-id string)"
    - "No data source or STS call is introduced (keeps the offline plan gate intact)"
  artifacts:
    - path: "modules/ecr/main.tf"
      provides: "Managed ECR repository for odoo-core with hardening defaults"
      contains: "resource \"aws_ecr_repository\""
    - path: "modules/ecr/outputs.tf"
      provides: "image_uri output = repository_url attribute"
      exports: ["image_uri"]
  key_links:
    - from: "modules/ecr/outputs.tf"
      to: "modules/ecr/main.tf"
      via: "image_uri value = aws_ecr_repository.<label>.repository_url"
      pattern: "repository_url"
---

<objective>
Implement `modules/ecr` as a **managed `aws_ecr_repository`** for the `odoo-core` image (per CONTEXT.md D-01/D-02/D-03 — NOT the rejected GHCR pull-through cache), with sensible hardening defaults, and export `image_uri` as the repository's `repository_url` attribute.

Purpose: Provides the AWS-native private image store the provisioner adapter pulls `odoo-core` from. Deriving the URI from the resource attribute keeps the offline `make plan-check` gate free of account-id variables and STS data sources (preserves Phase 1 D-06/D-07).

Output: Implemented `modules/ecr/main.tf` and `modules/ecr/outputs.tf`. Wiring into `envs/prod` is handled by plan 02-03 (Wave 2), so this plan does NOT touch `envs/prod`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-container-platform/02-CONTEXT.md
@.planning/phases/02-container-platform/02-PATTERNS.md

# Decision anchors (CONTEXT.md):
# - D-01: managed aws_ecr_repository, NOT a pull-through cache. NO GHCR credential, NO Secrets Manager, NO account_id var, NO aws_caller_identity.
# - D-02: image_uri = aws_ecr_repository.<label>.repository_url (resource attribute; no tag pinned; no hand-built <acct>.dkr.ecr string).
# - D-03: no Secrets Manager secret, no credential_arn, no account_id variable this phase.
# - Claude's Discretion: repo name under name_prefix (e.g. "${var.name_prefix}-odoo-core"); scan_on_push=true, image_tag_mutability, AES256 encryption, lifecycle policy to expire untagged images.

# IMPORTANT — the existing stub header in modules/ecr/main.tf carries REJECTED
# "pull-through cache (source: GHCR)" wording. Per D-01 it MUST be rewritten to the
# managed-repository decision, not just appended to.

<interfaces>
<!-- Module interface contract the root wiring (plan 02-03) depends on. -->
<!-- modules/ecr exposes exactly one output: -->
output "image_uri"  # value = aws_ecr_repository.<label>.repository_url
<!-- Sole input variable (already present in modules/ecr/variables.tf, do not change): -->
variable "name_prefix" { type = string }
<!-- Root expects: module.ecr.image_uri (see envs/prod/main.tf line 22-26, envs/prod/outputs.tf line 61-64) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the managed ECR repository and its outputs</name>
  <files>modules/ecr/main.tf, modules/ecr/outputs.tf</files>
  <read_first>
    - modules/ecr/main.tf (current stub — header carries REJECTED pull-through wording to be rewritten)
    - modules/ecr/outputs.tf (current stub — comment-only, to be replaced with a real output)
    - modules/ecr/variables.tf (the name_prefix input — already correct, do NOT modify)
    - modules/networking/main.tf (canonical module file structure: header comment, WHY-comment-above-each-resource, Name-tag-only pattern, "${var.name_prefix}-<thing>" naming)
    - modules/networking/outputs.tf (canonical output pattern: description ends with a period, value = resource attribute, no sensitive annotation)
    - bootstrap/main.tf (multi-resource-same-label pattern — supporting resources share the primary resource's logical label, e.g. aws_s3_bucket "tfstate" + aws_s3_bucket_versioning "tfstate")
  </read_first>
  <action>
    Rewrite modules/ecr/main.tf and modules/ecr/outputs.tf to implement a managed ECR repository (D-01/D-02/D-03).

    1. Rewrite the file-header comment in modules/ecr/main.tf following the networking header style (# Module: ecr / # Purpose: ... / SEED-001 note). The Purpose MUST describe a managed `aws_ecr_repository` for the odoo-core image (CI/CD pushes the image; AWS-native private storage). REMOVE all "pull-through cache" and "source: GHCR" wording and the STATUS-stub / TODO lines. Add a SEED-001 note explaining WHY managed-repo over pull-through per D-01 (no upstream GHCR credential, no Secrets Manager exception, repository_url is a resource attribute keeping the offline plan gate intact).

    2. Declare `resource "aws_ecr_repository"` with a logical label naming the role (e.g. `odoo_core`, snake_case — the label is the role, not the resource type). Set `name = "${var.name_prefix}-odoo-core"` (Claude's Discretion repo name under name_prefix). Add only a `Name` tag: `tags = { Name = "${var.name_prefix}-odoo-core" }` — Project/Environment/ManagedBy/Repo come from provider default_tags; do NOT re-declare them.

    3. Hardening defaults (Claude's Discretion D-32): set `image_tag_mutability` (choose IMMUTABLE for release-tag safety, with a one-line WHY comment), an `image_scanning_configuration { scan_on_push = true }` block (WHY: catch CVEs on push), and an `encryption_configuration { encryption_type = "AES256" }` block (KMS CMK deferred per CONTEXT deferred ideas — WHY comment). Each non-obvious block gets a single-line WHY comment above it.

    4. Declare `resource "aws_ecr_lifecycle_policy"` sharing the repository's logical label (multi-resource-same-label pattern from bootstrap). Reference the repo via `repository = aws_ecr_repository.<label>.name`. The policy JSON expires untagged images (e.g. keep rule: action type "expire", selection tagStatus "untagged", countType "sinceImagePushed" or "imageCountMoreThan"). Add a WHY comment (cost / registry hygiene). Use jsonencode() for the policy body, not a heredoc string.

    5. Replace modules/ecr/outputs.tf comment-only stub with a single `output "image_uri"` whose value is `aws_ecr_repository.<label>.repository_url` (D-02 — the resource attribute, NOT a hand-built account-id string, NO tag appended; the adapter appends the deployed tag). Description: one sentence ending with a period stating what it is, e.g. "ECR repository URL for the odoo-core image; the adapter appends the deployed tag." No `sensitive` annotation (a repo URL is not a secret).

    6. Introduce NO `data` sources (no aws_caller_identity, no aws_ecr_authorization_token) and NO new variables (D-03/D-07) — anything would break the offline plan gate or contradict the no-credential decision.
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep -q 'resource "aws_ecr_repository"' modules/ecr/main.tf && grep -q 'name_prefix}-odoo-core' modules/ecr/main.tf && grep -q 'scan_on_push' modules/ecr/main.tf && grep -q 'aws_ecr_lifecycle_policy' modules/ecr/main.tf && grep -q 'repository_url' modules/ecr/outputs.tf && ! grep -riq 'pull-through\|ghcr\|aws_caller_identity' modules/ecr/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `modules/ecr/main.tf` contains `resource "aws_ecr_repository"` with `name = "${var.name_prefix}-odoo-core"` and a `Name`-only tag.
    - `modules/ecr/main.tf` contains `image_scanning_configuration { scan_on_push = true }`, an `image_tag_mutability` attribute, and an `encryption_configuration` block with `AES256`.
    - `modules/ecr/main.tf` contains `resource "aws_ecr_lifecycle_policy"` referencing the repo by `.name` with a rule expiring untagged images, body built via `jsonencode`.
    - `modules/ecr/outputs.tf` defines `output "image_uri"` with `value = aws_ecr_repository.<label>.repository_url` and no `sensitive` annotation.
    - `grep -riq 'pull-through\|ghcr\|aws_caller_identity' modules/ecr/` returns NOTHING (rejected wording and STS calls fully removed).
    - No `data` block and no new `variable` block exist anywhere under `modules/ecr/`.
    - `terraform fmt -check modules/ecr` exits 0 (run `terraform fmt modules/ecr` first if needed).
  </acceptance_criteria>
  <done>
    The ecr module declares a managed, name_prefix-named, hardened ECR repository with an untagged-image lifecycle policy and exports `image_uri` from the `repository_url` attribute; no pull-through, GHCR, credential, account-id, data-source, or STS reference remains. `terraform fmt -check` is clean for the module. Note: a full `make plan-check` runs in plan 02-03 after wiring — do NOT wire envs/prod here.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD push → ECR | Image artifacts written into the private repository |
| ECS task pull → ECR | Tenant tasks pull the odoo-core image at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | odoo-core image tag overwrite | mitigate | `image_tag_mutability = IMMUTABLE` — release tags cannot be silently replaced |
| T-02-02 | Information Disclosure | Vulnerable image shipped to tenants | mitigate | `image_scanning_configuration { scan_on_push = true }` surfaces CVEs at push time |
| T-02-03 | Information Disclosure | Image layers at rest | mitigate | `encryption_configuration { encryption_type = "AES256" }`; KMS CMK deferred (low value at MVP) |
| T-02-04 | Denial of Service | Registry bloat / cost from untagged churn | accept→mitigate | `aws_ecr_lifecycle_policy` expires untagged images |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — Terraform providers resolve from the registry; not applicable |
</threat_model>

<verification>
- `grep -q 'resource "aws_ecr_repository"' modules/ecr/main.tf` (managed repo present)
- `grep -q 'repository_url' modules/ecr/outputs.tf` (output derives from attribute)
- `! grep -riq 'pull-through\|ghcr\|aws_caller_identity' modules/ecr/` (rejected approach + STS fully gone)
- `terraform fmt -check modules/ecr` exits 0
- Full offline gate (`make plan-check`) is verified in plan 02-03 after wiring.
</verification>

<success_criteria>
- modules/ecr declares a managed `aws_ecr_repository` (ECR-01) named with `var.name_prefix`, with scan-on-push, immutable tags, AES256 encryption, and an untagged-image lifecycle policy.
- modules/ecr exports `image_uri = aws_ecr_repository.<label>.repository_url` (ECR-02 module side).
- No GHCR / pull-through / credential / account-id / data-source / STS reference remains.
- Module formats clean (`terraform fmt -check`).
</success_criteria>

<output>
Create `.planning/phases/02-container-platform/02-01-SUMMARY.md` when done.
</output>
