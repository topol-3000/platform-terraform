---
phase: 04-shared-filesystem
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - envs/prod/main.tf
  - envs/prod/outputs.tf
autonomous: true
requirements:
  - EFS-02

must_haves:
  truths:
    - "module \"efs\" is uncommented in envs/prod/main.tf with all required arguments wired from module.networking"
    - "The efs_id output is uncommented in envs/prod/outputs.tf and resolves to module.efs.efs_id"
    - "make plan-check passes: terraform fmt -check, terraform validate, and terraform plan all succeed"
    - "The plan output includes aws_efs_file_system, aws_security_group (efs), and aws_efs_mount_target resources"
  artifacts:
    - path: "envs/prod/main.tf"
      provides: "Uncommented module \"efs\" call with name_prefix, vpc_id, task_security_group_id, subnet_ids_by_az"
      contains: "module \"efs\""
    - path: "envs/prod/outputs.tf"
      provides: "Uncommented efs_id output resolving to module.efs.efs_id"
      contains: "module.efs.efs_id"
  key_links:
    - from: "envs/prod/main.tf"
      to: "module.networking.private_subnets_by_az"
      via: "subnet_ids_by_az argument in module \"efs\" call"
      pattern: "subnet_ids_by_az.*module\\.networking\\.private_subnets_by_az"
    - from: "envs/prod/outputs.tf"
      to: "module.efs.efs_id"
      via: "uncommented efs_id output"
      pattern: "module\\.efs\\.efs_id"
---

<objective>
Wire modules/efs into envs/prod: uncomment and expand the module "efs" stub in envs/prod/main.tf, uncomment the efs_id output in envs/prod/outputs.tf, then run make plan-check to confirm the gate is green.

Purpose: Plan 01 implements the module; this plan activates it in the root config so the provisioner output contract gains efs_id and the plan remains green.

Output: Updated envs/prod/main.tf and envs/prod/outputs.tf; make plan-check passes with EFS resources in the plan.
</objective>

<execution_context>
@/home/yevhenii/.claude/get-shit-done/workflows/execute-plan.md
@/home/yevhenii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-shared-filesystem/04-CONTEXT.md
@.planning/phases/04-shared-filesystem/04-PATTERNS.md
@.planning/phases/04-shared-filesystem/04-01-SUMMARY.md

<interfaces>
<!-- Contracts established by Plan 01 and existing root config. Extracted from codebase. -->

From modules/efs/variables.tf (created in Plan 01):
  variable "name_prefix"            { type = string }
  variable "vpc_id"                 { type = string }
  variable "task_security_group_id" { type = string }
  variable "subnet_ids_by_az"       { type = map(string) }

From modules/efs/outputs.tf (created in Plan 01):
  output "efs_id" { value = aws_efs_file_system.main.id }

From modules/networking/outputs.tf (extended in Plan 01):
  output "private_subnets_by_az" { value = { for i, az in var.azs : az => aws_subnet.public[i].id } }

Existing root config locals (envs/prod/main.tf line 11):
  local.name_prefix = "${var.project}-${var.environment}"

Existing networking module outputs available at root:
  module.networking.vpc_id
  module.networking.task_security_group_id
  module.networking.private_subnets_by_az   (new, from Plan 01)

Stub to replace in envs/prod/main.tf (lines 84-87):
  # module "efs" {
  #   source      = "../../modules/efs"
  #   name_prefix = local.name_prefix
  # }

Stub to uncomment in envs/prod/outputs.tf (lines 51-54):
  # output "efs_id" {
  #   description = "Shared EFS id -> provisioner `aws_efs_id`."
  #   value       = module.efs.efs_id
  # }

Analog module call pattern (envs/prod/main.tf module "rds_tenant" block):
  module "rds_tenant" {
    source                 = "../../modules/rds-tenant"
    name_prefix            = local.name_prefix
    subnet_ids             = module.networking.private_subnet_ids
    vpc_id                 = module.networking.vpc_id
    task_security_group_id = module.networking.task_security_group_id
    ...
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire module "efs" into envs/prod/main.tf and uncomment efs_id output</name>
  <files>envs/prod/main.tf, envs/prod/outputs.tf</files>
  <read_first>
    - envs/prod/main.tf — read the full file; locate the commented module "efs" stub in the "# --- 6. Filestore, routing/TLS" section (lines ~84-87); understand surrounding context (what comes before/after) so the edit is surgical
    - envs/prod/outputs.tf — read the full file; locate the commented efs_id output (lines ~51-54)
    - .planning/phases/04-shared-filesystem/04-CONTEXT.md — D-05 (wiring rules: underscore label, name_prefix first, all wiring in envs/prod/main.tf, pre-declare variables), D-06 (offline only, module.networking.* refs)
    - .planning/phases/04-shared-filesystem/04-PATTERNS.md — envs/prod/main.tf section (exact expanded module call shape) and envs/prod/outputs.tf section (uncomment instruction)
    - .planning/phases/04-shared-filesystem/04-01-SUMMARY.md — confirm Plan 01 completed and the variable names it declared
  </read_first>
  <action>
    Make two targeted edits.

    Edit 1 — envs/prod/main.tf: Replace the commented module "efs" stub (the four lines starting with "# module \"efs\"") with a fully-expanded uncommented block. The expanded block passes all four module variables:
      - source = "../../modules/efs"
      - name_prefix = local.name_prefix (always first argument, per convention)
      - vpc_id = module.networking.vpc_id
      - task_security_group_id = module.networking.task_security_group_id
      - subnet_ids_by_az = module.networking.private_subnets_by_az

    Module label is "efs" (not "efs_filesystem" or any other label). Argument alignment uses spaces to align = signs within the block, matching existing module call style (terraform fmt will enforce this; match it pre-format). Place the block in the same location as the stub — within the "# --- 6. Filestore, routing/TLS" section, before the commented module "acm" block. Do not uncomment module "acm", "alb", or "route53" — those are Phase 5.

    Edit 2 — envs/prod/outputs.tf: Uncomment the efs_id output block (remove the four # comment markers on the output "efs_id" block at lines ~51-54). The description and value are already correctly formed ("Shared EFS id -> provisioner `aws_efs_id`." / module.efs.efs_id) — do not alter the content, only remove the leading # characters.

    Verify that no other commented outputs are accidentally uncommented. The alb_listener_arn, hosted_zone_id, and acm_cert_arn outputs must remain commented (Phase 5 scope).
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && grep -n "module.efs.efs_id\|subnet_ids_by_az" envs/prod/main.tf envs/prod/outputs.tf</automated>
  </verify>
  <acceptance_criteria>
    - envs/prod/main.tf contains an uncommented module "efs" block with subnet_ids_by_az = module.networking.private_subnets_by_az
    - envs/prod/main.tf module "efs" block has source = "../../modules/efs" and name_prefix = local.name_prefix
    - envs/prod/outputs.tf contains an uncommented output "efs_id" with value = module.efs.efs_id
    - The alb_listener_arn, hosted_zone_id, and acm_cert_arn outputs in envs/prod/outputs.tf remain commented out
    - The module "acm", module "alb", and module "route53" stubs in envs/prod/main.tf remain commented out
  </acceptance_criteria>
  <done>envs/prod/main.tf and envs/prod/outputs.tf are updated; efs module is active in the root config with all four arguments wired from module.networking</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run make plan-check and confirm EFS resources in plan</name>
  <files></files>
  <read_first>
    - Makefile — locate the plan-check target to understand what it runs (should be: terraform fmt -check + terraform validate + terraform plan with dummy-AWS env vars)
    - memory: [Terraform offline plan gate] — use make plan-check (not make plan), terraform lives at ~/.local/bin
  </read_first>
  <action>
    Run make plan-check from the repo root. The target runs terraform fmt -check (recursive), terraform validate, and terraform plan under the offline dummy-AWS environment. All three steps must pass.

    After a clean run, grep the plan output for the three EFS resource types to confirm they appear:
      - aws_efs_file_system.main
      - aws_security_group.efs
      - aws_efs_mount_target.main

    If terraform fmt -check fails: run make fmt first, then re-run make plan-check. If terraform validate fails: read the error, fix the specific attribute or reference causing it, then re-run. Do not run terraform apply under any circumstances.

    Note the total resource count in the plan output (it should be higher than after Phase 3 by the number of EFS resources added: 1 filesystem + 1 SG + N mount targets where N = number of AZs, typically 2).
  </action>
  <verify>
    <automated>cd /home/yevhenii/projects/saas/platform-terraform && make plan-check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - make plan-check exits 0
    - terraform fmt -check passes (no formatting errors)
    - terraform validate passes (no configuration errors)
    - terraform plan output is non-empty and includes "aws_efs_file_system.main", "aws_security_group.efs", and "aws_efs_mount_target.main[...]" resource entries
    - Plan output shows no errors about undeclared variables or unsupported arguments in the efs module
    - make plan-check output does not contain "Error:" anywhere
  </acceptance_criteria>
  <done>make plan-check exits 0 with EFS resources appearing in the terraform plan; the offline gate is green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root config → EFS module | All EFS inputs flow from module.networking outputs (no hardcoded values, no CIDR strings) |
| Terraform outputs → provisioner | efs_id (filesystem id) is non-sensitive; the output contract must not expose secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | envs/prod/main.tf wiring | mitigate | All networking refs via module.networking.* (no hardcoded subnet ids or SG ids); pre-declare all module variables per CONCERNS.md pattern before uncommenting the call |
| T-04-06 | Information Disclosure | envs/prod/outputs.tf efs_id output | accept | efs_id is the filesystem's AWS resource id — non-sensitive, safe to expose in outputs; no master passwords, tokens, or keys appear in outputs |
| T-04-07 | Denial of Service | module "acm"/"alb"/"route53" stubs accidentally uncommented | mitigate | Explicitly verify in acceptance criteria that those three stubs remain commented; their variables (tenant_domain, acm_cert_arn) are not declared for use in Phase 4, so an accidental uncomment would fail validate immediately |
| T-04-SC | Tampering | No package manager installs in this plan | accept | Pure HCL edits and a CLI gate run — no npm/pip/cargo installs; no supply-chain surface |
</threat_model>

<verification>
Full phase verification after both plans complete:

1. make plan-check exits 0 (terraform fmt -check + terraform validate + terraform plan all pass).
2. terraform plan output contains:
   - aws_efs_file_system.main (1 resource — encrypted, generalPurpose, elastic)
   - aws_security_group.efs (1 resource — NFS 2049 from task SG)
   - aws_efs_mount_target.main[...] (one per AZ, typically 2 — keyed by AZ name)
3. No cidr_blocks in the EFS SG ingress block (grep returns 0 matches for cidr_blocks within the efs SG ingress context).
4. envs/prod/outputs.tf has output "efs_id" uncommented and resolving to module.efs.efs_id.
5. alb_listener_arn, hosted_zone_id, acm_cert_arn remain commented (Phase 5 scope).
6. EFS resource count in plan is greater than Phase 3 count (confirms new resources added, plan is non-empty and growing).
</verification>

<success_criteria>
- module "efs" is uncommented in envs/prod/main.tf with subnet_ids_by_az = module.networking.private_subnets_by_az
- efs_id output is uncommented in envs/prod/outputs.tf
- make plan-check exits 0
- terraform plan shows aws_efs_file_system, aws_security_group (efs), and aws_efs_mount_target resources
- Phase 5 stubs (acm, alb, route53, their outputs) remain commented
</success_criteria>

<output>
Create `.planning/phases/04-shared-filesystem/04-02-SUMMARY.md` when done.
</output>
