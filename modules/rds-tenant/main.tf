# Module: rds-tenant
# Purpose: Shared tenant RDS PostgreSQL (Single-AZ), database-per-tenant
#
# SEED-001 note: Size on max_connections (~5-20 conns/tenant), NOT 100 DBs/instance. Adapter creates DB+role per tenant.
#
# STATUS: stub. No resources yet.
# See ../../../provisioner/.planning/seeds/SEED-001-aws-real-deployment.md
#
# TODO: implement Shared tenant RDS PostgreSQL (Single-AZ), database-per-tenant
