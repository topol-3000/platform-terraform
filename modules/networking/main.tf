# Module: networking
# Purpose: VPC, public subnet (NO NAT gateway), security groups
#
# SEED-001 note: Public subnet, no NAT (cost). Tenant task SG must accept 8069 ONLY from the ALB SG (proxy_mode trusts X-Forwarded-*).
#
# STATUS: stub. No resources yet.
# See ../../../provisioner/.planning/seeds/SEED-001-aws-real-deployment.md
#
# TODO: implement VPC, public subnet (NO NAT gateway), security groups
