# Module: ssm
# Purpose: SSM Parameter Store SecureStrings (HMAC salt, RDS master creds, tokens)
#
# SEED-001 note: Parameter Store, NOT Secrets Manager (~16-20x cheaper; HMAC passwords are reproducible).
#
# STATUS: stub. No resources yet.
# See ../../../provisioner/.planning/seeds/SEED-001-aws-real-deployment.md
#
# TODO: implement SSM Parameter Store SecureStrings (HMAC salt, RDS master creds, tokens)
